# 1. 09:30 ~ 10:20
### # 수업 전 할 일
단톡방에 폴더 다운 - 압축풀기
새로운 directory생성 (D_jQuery) 안에 압축 푼 파일에 들어있는데 HTML문서 1개넣고
압축 푼 폴더에 들어 있는 사진 2개는 images폴더에 넣기


### # jQuery의 상위 개념?이 JS임
- 개념
JS의 라이브러리(Library)
기존의 CSS, 특히 선택자하고 JS하고 같이 묶어서 코딩하는 것을
집대성해서 라이브러리 형태로 만들어 놓은것
* 요즘은 당연히 사용 할 줄 알아야 함 *



### # Library (라이브러리) / Framework (프레임워크) (오픈채팅 교육자료 사진 참고)



### # (D_jQuery)에 jQueryImageTestNew 파일 생성

### # 부트스트랩과 jQuery
부트스트랩의 전제가 jQuery의 존재라서
부트스트랩을 쓴다면 jQuery를 적어주는 것이 좋다 (반드시! 무조건!은 아니지만)



### # jQuery 제이쿼리 (jQueryImageTestNew 파일)
W3Schools에서 JQUERY클릭
https://www.w3schools.com/jquery/default.asp

Examples in Each Chapter 예제 Try it Yourself누르고
https://www.w3schools.com/jquery/tryit.asp?filename=tryjquery_hide

부트스트랩의 cdn과 같은
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
이 구문을
jQueryImageTestNew에 <head>에 넣기
그 밑에 부트스트랩 cdn 넣기
 (실습)
 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <style></style>
</head>
<body class="container mt-4">

<script>

</script>

</body>
</html>

-> 기본 골조 만드는 것


# 2. 10:30 ~ 11:20
### # (jQueryImageTestNew 파일) 이어서 작성

### # transition 속성
주어진 기간동안 속성값을 바꾸는 것
ex) 마우스를 이미지에 가져다 대면 이미지의 속성값인 width와 height가 커져서 웹페이지상 크게 보임
주어진 기간 : 마우스가 이미지 위에 있는 동안
속성값 : width, height
바뀜 : 커짐

ex) 마우스를 이미지에 가져다 대면 이미지의 속성값이 달라져서 웹페이지상 크게 보이게 하는 것
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <!-- jQuery Library Load -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .img-box{
            width: 250px;
            height: 160px;
            margin: 10px;
            cursor: pointer; /* 마우스 커서 포인터 */
            transition: 0.3s;              // 0.3초후 변함 (변화는 과정을 0.3초간 보여줌)
        }
        .zoom:hover{
            transform: scale(1.1);            // 1.1배의 크기만큼 커짐
        }
    </style>
</head>
<body class="container mt-4">
<h3>이미지 활용 하기</h3>

<h5>Hover 확대</h5>
<img src="/images/brioche_02.png" class="img-box zoom">

<script>
</script>

</body>
</html>


### # W3Schools에서 CSS Pseudo-classes 따로 공부하기!!!!!!!
https://www.w3schools.com/css/css_pseudo_classes.asp

### # Pseudo-classes
기존 선택자 뒤에 콜론(:)을 붙이고 가상 클래스 이름을 적습니다.

- 문법 :
(CSS)
선택자:가상클래스 {
  속성: 속성값;
}

- 종류들
1) 반응형
:hover	요소에 마우스를 올렸을 때	버튼 색상 변경, 메뉴 펼치기
:active	요소를 클릭하고 있는 동안	버튼이 눌리는 효과
:focus	요소에 포커스가 갔을 때 (입력창 등)	input 테두리 강조
:visited	사용자가 이미 방문한 링크일 때	읽은 글 표시

2) 구조적
:link - Unvisited links
:nth-child(n)	부모 안에서 n번째 자식일 때	표(table)의 짝수 줄 색칠하기
:first-child	첫 번째 자식 요소일 때	목록의 첫 항목 테두리 제거
:last-child	마지막 자식 요소일 때	목록의 마지막 구분선 제거



### # jQuery이용하기 (jQueryImageTestNew 파일)
- 비교하기
1) document(HTML)의 모든 내용을 읽었을때(이벤트 발생) function을 실행하라는 동작
표준 자바스크립트 (Vanilla JS) : document.addEventListener("DOMContentLoaded", ...)
jQuery 방식 : $(function() { ... });

누구를 (선택자입력)

* $ 기호는 jQuery를 부르는 기호 *



### # 클릭해서 변하는 토글 생성하기 (중요!!!!!)
- $(function() { ... });
HTML)의 모든 내용을 읽고나서 function을 실행하라는 동작

- $("")
""안에 CSS에 선택자 넣는 것처럼 선택자를 심볼과 함께 넣으면 됨
ex) $("#grayImg")
-> id가 grayImg인 태그를 의미함! (중요!!)

- $("").click(function(){});
선택자가 ""인 태그가 click될때 function의 {}을 실행해라

- $(this)
this는 부모의 것과 같은 선택자
그 선택자인 태그를 의미함!
ex) $(this)

- toggleClass("");
""라는 이름의 Class를 추가해라
ex) $(this).toggleClass("gray");
부모 선택자인 태그안에 class="gray"를 토글형식으로 추가/삭제해라

전체 ex)
<script>
    // $(function(){여기에할일작성});
    $(function (){
        // 흑백 토글
        $("#grayImg").click(function(){
            $(this).toggleClass("gray");
        });
    });
</script>

- 풀이
HTML을 읽고 다음 동작을 실행해라
grayImg라는 이름인 id 선택자인 태그를 클릭할때
grayImg라는 이름인 id 선택자인 태그 안에 gray라는 이름인 class 선택자를 토글형식으로 추가/삭제해라


# 3. 11:30 ~ 12:20
### # (jQueryImageTestNew 파일)에 ((jQueryImageTest 파일의 일부 코드 복붙)
- 아래의 코드를 <body>코드에 복붙

<h5>3. 선택 이미지 강조</h5>
<div>
    <img src="/images/croissant_02.png" class="img-box select-img">
    <img src="/images/cappuccino02.png" class="img-box select-img">
    <img src="/images/wine01.png" class="img-box select-img">
</div>
<hr>



### # 활성화 되는 것을 표현
<style>
	/* 특정 항목이 활성화 되었을 때 */
	.active{border: 2px solid blue;}
</style>

.active 클래스는 개발자들 끼리의 약속이여서 사실상 이름은 바꿔도 상관없음



### # 이미 활성화된 것을 비활성화 시키고 다시 선택한 것을 활성화 시키기
- addClass("")
클래스 추가

- removeClass("")
클래스 삭제


* 기본 양식 *
$("").click(function (){
	
});

(선생님이 만든 코드)
$(".select-img").click(function (){
	$(".select-img").removeClass("active"); // $(".select-img")인 모든 애들 삭제함 (이 파일에서는 3개의 파일을 다 확인해야함)
	$(this).addClass("active");
});

(내가 만든 코드)
$(".select-img").click(function (){
	$(".active").removeClass("active");  // $(".active")인 친구를 하나만 확인하면 되는거여서 더 최적화된 코드임
	$(this).addClass("active");
});



### # (jQueryImageTestNew 파일)에 ((jQueryImageTest 파일의 일부 코드 복붙)
- 아래의 코드를 <body>코드에 복붙

<h5>4. 카테고리 필터</h5>
<select id="categorySelect" class="form-select w-25 mb-2">
    <option value="all">전체 보기</option>
    <option value="bread">빵</option>
    <option value="drink">음료수</option>
    <option value="wine">와인</option>
</select>

<div>
    <img src="/images/brioche_02.png" class="menu-img" data-category="bread">
    <img src="/images/french_baguette_01.png" class="menu-img" data-category="bread">
    <img src="/images/americano01.png" class="menu-img" data-category="drink">
    <img src="/images/cappuccino02.png" class="menu-img" data-category="drink">
    <img src="/images/whitewine02.png" class="menu-img" data-category="wine">
    <img src="/images/wine01.png" class="menu-img" data-category="wine">
</div>
<hr>

- 콤보박스이용
click(){}을 사용해도 되지만
change(){}를 사용하는게 좋음 (항목이 바뀌었다는 의미에서)
이런 함수를 이벤트라고 부름
click이벤트
change이벤트 : 값이 바뀌면 동작 실행

- 실습
$("#categorySelect").change(function (){
	const selected = $(this).val();
	console.log(`선택 항목 : ${selected}`);
});

(풀이)
categorySelect라는 이름의 id선택자가 있는 태그가 변하면 기본값이 value="all" 이거임
categorySelect라는 이름의 id선택자가 있는 태그의 value값을 selected객체에 넣고
선택 항목 : ${selected}를 콘솔에서 보여줘라

* change이벤트가 바뀌는 주체를 자동으로 인식하는 이유는 select태그의 값이 option value라는 것으로 정해져 있기 때문에 *

# 4. 12:30 ~ 13:20
### # (jQueryImageTestNew 파일) 이어서 하기 (콤보박스이용해서 카테고리 별 제품들만 보여주기)
- show() 이벤트
해당 객체를 보여주는 이벤트

- hide() 이벤트
해당 객체를 숨겨주는 이벤트

- 속성 선택자를 사용해서 해결하기
특정 클래스의 data-category의 값이 특정 값인 것들만 찾으면 됨 (중요함!!!)
선택자뒤에 대괄호를 넣으면 필터링이 가능함

1)문법 :
.선택자[필터링할내용]

2) 이용하기) 클래스가 combo-image인 것들 중에서 / data-category라는 속성이 있는데 / 그 값이 특정값인 것들 찾기
ex) $(".combo-image[data-category='" + selected + "']").show();
.combo-image라는 이름의 클래스가 가진 data-category속성 값이 selected변수값과 동일한 태그를 의미함

**** (풀이과정) (중요함!!!!!) ****
전제 : $의 소괄호인 ()안에는 꼭! 따옴표가 들어가야할 필요는 없고 문자열 데이터만 들어가면 됨!
 
$(".combo-image[data-category='" + selected + "']");

1. -> 각 섹션들이 문자열로 바뀜
1).combo-image[data-category='
*)+ 결합연산자
2) selected변수가 가진 문자열 값인 all로 바뀜 -> all
*)+ 결합연산자
3) ']

2. - 문자열로 바뀐 것들이 결합연산자로 연결됨
-> .combo-image[data-category='all']

* 중요 포인트 : 원래 처음에는 외따옴표가 서로 다른 쌍따옴표의 영역에 들어가 있기 때문에 쌍으로 연결 되지 않아서 사실상 외따옴표 역할을 하지 않는
그냥 문자의 역할을 했지만 전체 구문이 문자열로 바뀌고 결합연산자로 연결되면서 드디어 제 역할을 하는 외따옴표 쌍이 완성되었음!!!!!*
* 마지막 완성된 문장을 봐도 사실상 외따옴표가 필요하지는 않을 것 같지만 ('all'이나 all이나 같으니까)
만약 외따옴표가 없는 상태에서 변수가 가진 문자열 값이 공백을 포함하고 있는 inner peace같은 문자열 데이터면
.combo-image[data-category=inner peace]
이런식으로 나오게 되는데
이러면 에러가 발생할 수 있기 때문에 변수가 공백이 있는 문자열 값을 가지고 있어도 에러가 나지 않게 하려고 외따옴표를 굳이 붙여서
.combo-image[data-category='inner peace']
이렇게 만들어서 마지막으로 한번 더 문자열로 만들어주는 것이다.

* 중요 포인트!!!! 외따옴표의 신분변화 (일반 의미없는 기호 -> 외따옴표 역할하는 특별한 기호) *

(전체 실습)
$("#categorySelect").change(function () {
	const selected = $(this).val(); // 선택한 품목명
	console.log(`선택 항목 : ${selected}`);

	if (selected === "all") {
		$(".combo-image").show();
	} else {
		$(".combo-image").hide();
		$(".combo-image[data-category='" + selected + "']").show();
	}
});



### # W3Schools에서 jQuery Selectors찾기
jQuery의 선택자들?
- 확장자가 .jpg인 것들만 가져오기
[attribute$=value]	$("[href$='.jpg']")	All elements with a href attribute value ending with ".jpg"

- 시작이 "Tom"으로 시작하는 거
[attribute^=value]	$("[title^='Tom']")	All elements with a title attribute value starting with "Tom"

# 5. 14:20 ~ 15:10
### # (jQueryImageTestNew 파일) 이어서 하기 (<!-- 5️⃣ 썸네일 → 큰 이미지 -->)
(CSS)
#mainImage{
	width: 100%;
	height: 300px;
	max-width: 500px;
	transition: transform 0.3s ease;
	transform-origin: top left;
}

#mainImage:hover{
	transform: scale(1.1);
}

.active-thumb{border: 2px solid blue;}

(HTML)
<h5>5. 썸네일 → 큰 이미지 변경</h5>
<img id="mainImage" src="/images/americano01.png" class="img-fluid mb-2">
<div>
    <img src="/images/americano01.png" class="img-box thumb-image active-thumb">
    <img src="/images/whitewine02.png" class="img-box thumb-image">
    <img src="/images/brioche_02.png" class="img-box thumb-image">
</div>
<hr>

(JS)
$(".thumb-image").click(function(){
	const newSrc = $(this).attr("src");
	console.log(`선택된 이미지 : ${newSrc}`);
	$("#mainImage").attr("src", newSrc);
	$(".active-thumb").removeClass("active-thumb");
	$(this).addClass("active-thumb");
});


### # 속성 attribute는 jQuery에서 attr임

# 6. 15:20 ~ 16:10
### # (jQueryImageTestNew 파일) 이어서 하기 (<!-- 6. 이미지 회전 (이전 / 다음) -->)

### # id가 rotateBox인 태그의 자식들 중 img태그인 자식들을 다 가져오기 
- $("#rotateBox img") - 3개임

- $("#rotateBox img").last();                 <----> 반대는 first()
-> 가장 마지막 순서의 img태그를 가져옴 

- append
제일 뒤에 넣기

- prepend
제일 앞에 넣기


### # (jQueryImageTestNew 파일) 이어서 하기 (<!-- 7. 이미지 스타일 변경 -->)
- type="radio"
한개의 그룹안에 있는 여러 개중에서 한개를 선택할때
한개의 그룹으로 지정하는 기준 : name속성값이 같으면 한개의 그룹임 (그룹핑)
* 반드시 name속성값을 같게 해야 됨 *

- 체크박스, 라디오, 콤보박스는 click보다는 change이벤트로 많이 코드를 만드는 경향이 있음

-
$("input[name='imgStyle']").change(function (){
	
});

input태크 중에서 name속성이 imgStyle속성값을 가지는 것

# 7. 16:20 ~ 17:10
### # (jQueryImageTestNew 파일) 이어서 하기 (<!-- 7. 이미지 스타일 변경 -->)
myImage.removeClass().addClass(img-fluid);
class안에 있는거 다 지우고 img-fluid만 넣기
동작 2개 하기

### # method chaining
동작 2개 이상 적었을때

### # 휴식

# 8. 17:20 ~ 18:10

VCS

MySQL
