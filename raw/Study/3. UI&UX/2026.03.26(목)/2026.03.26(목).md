# 1. 09:30 ~ 10:20
### # 오늘 수업 전에 공부할 것
관련 파일 : ProductList.html

자바 스크립트 관련
배열의 forEach() 함수(자바의 확장 for와 유사함)
화살표 함수(arrow function)
confirm(), alert() 함수
DOM(Document Object Model)
template string



### # 상품 목록 페이지 만들기 (with bootstrap) (웹 서비스 Ui&UX 교안 (P.33))
- 배열 생성, 원소 하나 만들기
<script>
    /* 상품_목록 */
    const products = [ // products배열
      { id:1, name:'아메리카노', price:4000, image:'/images/americano01.png'} // 원소 하나 생성
    ];
</script>


- 사용자 정보 입력
/* 사용자 정보 */
const user = {
  role: 'ADMIN' // USER, ADMIN
}
-> role 속성의 의미 (사실 내가 그렇게 이름을 정한거지 name: 과 별 다를바 없이 특별한 자체 기능은 없고 그냥 관례적으로 role이라고 적은 것)
역할 정의: 시스템 내에서 허용된 행동의 범위를 결정합니다.
권한 제어: 예를 들어, 게시글을 지우거나 사용자 목록을 볼 수 있는 권한은 role: 'ADMIN'인 사용자에게만 주도록 로직을 짤 수 있습니다.
-> 로직이 없으면 name과 같이 특별한 기능이 없는 그냥 이름임


* bootstrap의 경우 container라고 class명을 붙여야 레이아웃을 맞춰줌 *
ex)
<div class="container my-4">
  
</div>

- 관리자 전용 버튼 생성
<div class="d-flex justify-content-start">
	<a href="/product/insert"> // "/product/insert"으로 이동한다.
	  <button id="insertBtn" class="btn btn-primary mb-3">상품 등록</button> // 링크로 이동하는 버튼 생성
	</a>
</div>


- 관리자 전용 버튼이 관리자한테만 보이게 설정하기
1) (role이 ADMIN일때만 "관리자 전용 버튼"이 보이게 설정하기)
JS (<script>태그 내부)에서 if문을 생성하기
객체의 속성 => user.role이고 현재 이 값은 ADMIN임

2) 버튼
id는 하나뿐이니까 id를 이용해서 구분을 버튼을 JS에 가져와야 함
ex)
/* 관리자가 아니면 상품 등록 버튼 숨길 */
if(user.role !== 'ADMIN'){
  // 화면에서 안보이고, 실제 공간도 없어짐
  document.getElementById("insertBtn").style.display = "none";
}
-> document는 기본 객체(HTML의 객체) / getElementById("insertBtn")가 사실상 버튼 / style은 속성?외관? / display보이는것 / "none"은 실제로 없어짐

(실습)
JS안에 role을 USER로 바꾸면 실제로 웹페이지에서 버튼이 없어짐

* JS상에서 HTML의 객체는 document니까 HTML의 id나 클래스같은걸 불러올때 document를 사용해서 가져오면 됨 *

- 상품 목록에 추가하는 동작을 위해 상품 목록 태그를 JS로 가져오기
const container = document.getElementById("productContainer");

- 상품_배열을_반복하여 목록 생성하기 (JS의 forEach이용하기)
1) forEach함수
JS의 forEach함수는 Java의 향상된 for구문과 비슷한 느낌
원소 각각을 개별적으로 동작시켜서 결과값을 추출한다.

### # W3Schools에 JavaScript Array forEach() 검색해서 공부
https://www.w3schools.com/JSREF/jsref_forEach.asp#gsc.tab=0

# 2. 10:30 ~ 11:20
### # W3Schools에 JavaScript String Templates 검색해서 공부
https://www.w3schools.com/js/js_string_templates.asp

### # JavaScript String Templates
- templates string (Java의 printf같은 포맷형으로 문자열 결합하기)
문법 :
${...}는 띄어쓰기가 없음
let firstName = "John";
let lastName = "Doe";

let text = `Welcome ${firstName}, ${lastName}!`;
-> text의 데이터 값은 Welcome John, Doe! 임
-> 외따옴표나 쌍따옴표말고 ` ` (백틱(Backtick) 사용하기)
${} : 안에 있는 것을 수식표현(변수)로 인식함 (문자열로 인식하지 않음)
-> 문자열이 아닌 형식의 형태안에 문자열의 데이터가 있을때 이 데이터를 가져올때 사용함


### # W3Schools에 Arrow Functions 검색
https://www.w3schools.com/js/js_arrow_function.asp
Before Arrow 찾기 (Java의 메소드의 구성요소를 생각하면 이해하기 편함) (메소드명, 반환타입, 매개변수)
* Java에서 메소드였다면 JS에서는 함수라고 부름 *

const multiply = function(a, b) {return a * b}
-> 함수명 : multiply / 매개변수 : a, b / 반환타입 : a * b의 타입

축소버전 : const multiply = (a, b) => a * b;
-> 함수명 : multiply / 매개변수 : a, b / a * b의 타입

* Arrow function(=>)은 약식으로 만든다는 것을 의미함 *


### # - 상품_배열을_반복하여 목록 생성하기 (JS의 forEach이용하기)
/*상품_배열을_반복하여 목록 생성하기*/
products.forEach(item => {
        console.log(item.name);
    })
1) products배열(여러개에서)에서 item(1개인 매개변수가)이 들어옴
-> item은 products배열 안에 있는 배열 요소들

2) item.name을 하면 배열 요소안에 적힌 name이라는 데이터를 불러 올 수 있음

3) console.log(item.name); 
-> console.log()	괄호 안의 내용을 출력하는 함수 (console은 브라우저의 f12개발자 도구의 console을 의미하는게 맞음)
-> item	데이터가 담겨 있는 객체(Object)
-> name	객체 안에 저장된 특정 속성(Key)

4) 크롬에 F12(개발자 도구) 눌러서 Console들어가보기


5) JS에서 동적으로 태그를 만들때 createElement()함수를 이용함
// createElement : 자바 스크립트로 태그를 동적으로 생성
const col = document.createElement("div")
-> JS상에서 col이라는 객체?에 <div>태그를 동적으로 생성

col.className = "col-md-4 mb-4";
-> col(<div>태그인)에게 class이름을 "col-md-4 mb-4"라고 부여함

col.innerHTML = `${item.name}`;
-> col(<div class="col-md-4 mb-4"></div>)) 안에 forEach로 반복되면서 계속해서 item.name이 문자열 데이터들이 들어감
-> innerHTML이라서 눈에는 안보이지만 HTML안에 존재하는 <div class="col-md-4 mb-4"></div>태그에 들어감


위의 작업들은 사실상 HTML에 <div class="col-md-4 mb-4">item.name안의_0문자열_데이터값</div>과 동일한 결과
근데 JS에서 만드는 이유는 동작을 넣기 위해서 (forEach를 사용하기 위해서)


6) <div class="col-md-4 mb-4">item.name안의_0문자열_데이터값</div>을 container에 추가하기
사실상 부모안에 자식 요소들을 추가하는 것 (appendChild함수 사용)
container.appendChild(col)

(HTML에 아래와 같이 변함 - 눈에는 <div class="col-md-4 mb-4"></div>이 안보이지만)
<div class="row" id="productContainer">
	<div class="col-md-4 mb-4"></div>
</div>

* const container = document.getElementById("productContainer"); * (중요함!!!!!)
이거때문에 JS상의 container객체가 바뀌면 HTML상의 id="productContainer"도 같이 바뀜

# 3. 11:30 ~ 12:20
### # 관리자일때 수정, 삭제 버튼 추가 (백틱 사용 유의)
if (user.role === 'ADMIN') {
	const adminDiv = document.getElementById(`admin-${item.id}`);
	adminDiv.innerHTML = `
		<button class="btn btn-warning btn-sm mb-1" onclick="event.stopPropagation(); edit(${item.id})">수정</button>
		<button class="btn btn-danger btn-sm" onclick="event.stopPropagation(); remove(${item.id}, '${item.name}')">삭제</button>
	`;
}


### # 돔 트리
Dom (Document Object Model)

### # W3Schools에 appendChild 검색하기
HTML DOM Element appendChild() Method
https://www.w3schools.com/jsref/met_node_appendchild.asp

- append : 무리가 있는데 그 무리 제일 뒤쪽에 하나 추가하는 행위

- node : 태그의 프로그래밍적 이름
우리는 눈에 보이는 것을 <div>나 <h1> 같은 태그라고 부르지만,
자바스크립트 입장에서는 이들을 조작 가능한 **객체(Object)**로 취급합니다.
그래서 이를 **노드(Node)**라고 부르는 거예요.
특징: 태그뿐만 아니라 그 안에 들어가는 글자(Text), 주석 등도 전부 넓은 의미의 노드에 포함됩니다.

- create : 생성 - 메모리 상에 새로운 노드를 탄생시키는 것



### # on
on+이벤트 조합으로 사용
- 마우스 클릭 : click 이벤트
ex) 태그 안에 onclick="register()"
클릭했을때 register()함수가 동작해라
ex)
<div class="d-flex justify-content-start">
	<a href="/product/insert">
		<button id="insertBtn" onclick="register()" class="btn btn-primary mb-3">상품 등록</button>
	</a>
</div>


### # 함수 만들기
문법 :
function 함수명(){.....}
ex)
<script>
function register(){
	alert('상품 등록 완료')
}
</script>
-> alert라는 이벤트를 발생하는 register함수 생성


# 4. 12:30 ~ 13:20
### # 이벤트 (HTML&JS&CSS 이론(new) 교안 (P.213))
- 이벤트(event) : 브라우저에서 발생하는 사건들
ex) 키보드 누르기, 마우스 클릭, 스크롤, 페이지 전환 등

- Event Driven Programming
특정 이벤트에 따라서 실행 코드를 작성하는 기술을 의미

- Event 이름 click, load, unload 등등
Event Type라고도 불림

- Event 속성
on + load(on + 이벤트 이름)

- 이벤트 핸들러 (Event Handler)
이벤트 속성에 할당이 되어 있는 함수
[어떤 어떠한 이벤트가 발생하면… ~해라.] 라는 식으로 해석

- Event의 종류
키보드, HTML 프레임, 유저 인터페이스, 구조 변화, 터치 이벤트

- ex)
예를 들어서, 마우스 버튼을 누르면 mousedown 이벤트가 발생하고,
마우스 버튼을 떼면 mouseup 이벤트가 발생한 다음 바로
click 이벤트가 발생합니다.

### # 추가적인 이벤트(event) 내용 공부는 W3Schools이용하기


### # 브라우저(Browser) 객체 (HTML&JS&CSS 이론(new) 교안 (P.224~225))
따로 교안 보고 공부하기
암기X / 이해하기



### # 상품 목록 페이지 만들기 (with bootstrap) (웹 서비스 Ui&UX 교안 (P.33)) 이어서 하기
- 버튼에 함수 적용하기 (register, goDetail, edit, remove)


# 5. 14:20 ~ 15:10
### # 상품 상세보기 만들기 (with bootstrap) (웹 서비스 Ui&UX 교안 (P.30~31))
(ProductDetail 파일 생성)

* 숫자도 쌍따옴표 붙어야 함 *

- 버튼들 만들기
1) 생성
<div class="container my-4">
<div class="mb-3">
	<button class="btn btn-secondary btn-sm" onclick="showLoading()">Loading</button>
	<button class="btn btn-danger btn-sm" onclick="showError()">No Data</button>
	<button class="btn btn-success btn-sm" onclick="showSuccess()">Success</button>
</div>

<!-- 로딩 화면 -->
<div id="loadingView" class="text-center">
	<h3>상품 정보를 로딩중입니다.</h3>
</div>

<!-- 데이터 없음 -->
<div id="errorView" class="text-center" style="display: none;">
	<h3>상품 정보를 찾을 수 없습니다.</h3>
</div>

<!-- 정상 화면 -->
<div id="successView" style="display: none;">
	상품 상세 보기
</div>
</div>

2) 동작
function showLoading(){
	document.getElementById("loadingView").style.display="block";
	document.getElementById("errorView").style.display="none";
	document.getElementById("successView").style.display="none";
}
function showError(){
	document.getElementById("loadingView").style.display="none";
	document.getElementById("errorView").style.display="block";
	document.getElementById("successView").style.display="none";
}
function showSuccess(){
	document.getElementById("loadingView").style.display="none";
	document.getElementById("errorView").style.display="none";
	document.getElementById("successView").style.display="block";
}



### # W3Schools에 BS5 Cards에 Header and Footer이용


# 6. 15:20 ~ 16:10
* form은 입력!!! 양식임 *
그냥 표같은건 table을 사용하는게 좋음

### # 상품 상세보기 만들기 (with bootstrap) (웹 서비스 Ui&UX 교안 (P.30~31)) 완성함

# 7. 16:20 ~ 17:10
### # 웹 페이지 주소 형식 (IT 관련 용어 교안 (P.26))
(LoginTest 파일 생성)
(선생님 카톡 복붙)

- Parameter
주소창에 ?이후에 있는 것들
사용자에게 넘겨줄 데이터?들
시작점 : ?
구분자 : &
Parameter의 수 : 구분자의 수 - 1
Parameter이름과 값은 =로 구분되어 있음

http://localhost:63342/login?username=hong&password=hello
hong, hello가 Parameter임


### # 전송방식 (method)
1) get
- 엽서에 적어서 보내는 방식
단점 : 내용을 다 볼 수 있음 (보안 취약) -> username이랑 password가 주소창에 다 보임
ex) http://localhost:63342/login?username=hong&password=hello

2) post
- http : 프로토콜 (2매체가 통신을 하기 위한 통신 규약)
http도 head가 있고 body가 있는데 post방식은 http의 body에 Parameter가 붙어서 가서 안보임
장점 : 내용을 주소창에서 볼 수 없음 (보안 유리)
ex) http://localhost:63342/login


### # (ProductList파일에서)
function goDetail(id){
	//alert('상세 페이지 이동 :' + id);
	location.href = `ProductDetailNew.html?id=${id}`;
}

location은 이동하게 하는 객체?


### # (ProductDetailNew 파일 생성)
(ProductList 웹페이지)
상품 사진을 누르면 ProductDetailNew의 웹페이지로 이동됨



### # DOMContentLoaded
HTML문서는 원래 top-down방식인데
순서를 비트는 구문
HTML문서가 다 읽혀지고나서 동작하라는 구문



### # Dummy data
정식 데이터가 아니고 한시적으로 만든 데이터



### # URLSearchParams
쿼리 문자열(파라미터 목록)을 key-value 형태로 분리해주는 함수


### # 웹페이지 대소문자
웹페이지는 무조건 대소문자를 구별함


# 8. 17:20 ~ 18:10
### # ???
const product = products.find(item => item.id === productId);
-> 내가 선택한 상품 id에 해당하는 상품 정보

const productId = Number(params.get("id"));
-> 넘어오는 Parameter를 숫자로 바꿔줘라 -> Number();

### # 내일 할 예정
제이쿼리
