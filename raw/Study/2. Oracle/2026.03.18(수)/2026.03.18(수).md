# 1. 09:30 ~ 10:20
### # 오라맨사용자 삭제 후 선생님이 보내주신 A03스크립트로 다시 오라맨 만들고 세팅하기
1. A01 스크립트 가져오기
1) 로컬DB(오라맨) 커넥션의 연결을 끊기
2) drop user oraman cascade ;을 Ctrl + Enter - oraman을 삭제하기
3) -- 사용자 계정 생성
-- 계정 생성하고 잠금을 풀어 줍니다.
create user oraman identified by oracle account unlock ;

-- 기본 테이블 저장소를 users로 변경합니다.
-- 할당량(quota)을 무제한(unlimited) 제공해 줍니다.
alter user oraman default tablespace users quota unlimited on users ;

-- oraman에게 접속 권한(connect)과 resource 권한을 부여합니다.
grant connect, resource to oraman ;

2. A03 스크립트(선생님이 주신 A03스크립트) 가져오기
1) Active datasource를 oraman으로 설정
2) 전체 코드 실행


### # 제약 조건 테스트
A02가져와서 "-- 제약 조건 테스트" Ctrl + F로 찾기
members에 데이터를 추가할때 members의 제약조건을 먼저 확인 후 추가해야함

-- 정상 데이터 입력 테스트 (-- 정상 케이스)
INSERT INTO MEMBERS(ID, NAME, PASSWORD, GENDER, BIRTH, MARRIAGE, SALARY)
VALUES('user01','홍길동','1234','남자',DATE '1990-01-01','미혼',300);

실행 -> 성공

-- PRIMARY KEY 중복 테스트
INSERT INTO MEMBERS(ID, NAME, PASSWORD, GENDER, BIRTH, MARRIAGE, SALARY)
VALUES('user01','김철수','1234','남자',DATE '1992-02-02','미혼',400);

실행 -> 오류코드(ORA -00001: unique constraint violated) -> ID가 primary key인데 ID가 중복되어서 실행불가

-- NOT NULL 테스트
-- ID NULL
INSERT INTO MEMBERS(ID, NAME, PASSWORD, GENDER, BIRTH, MARRIAGE, SALARY)
VALUES(NULL,'홍길동','1234','남자',DATE '1990-01-01','미혼',300);

실행 -> 오류코드(ORA-01400: cannot insert NULL) -> ID가 primary key인데 ID를 Null로 넣을 수 없어서 실행불가

-- MARRIAGE NULL
INSERT INTO MEMBERS(ID, NAME, PASSWORD, GENDER, BIRTH, MARRIAGE, SALARY)
VALUES('user02','홍길동','1234','남자',DATE '1990-01-01',NULL,300);

실행 -> 오류코드(ORA-01400: cannot insert NULL) -> marriage를 null로 넣을 수 없어서 실행불가

-- 정상
INSERT INTO MEMBERS(ID, NAME, PASSWORD, GENDER, BIRTH, MARRIAGE, SALARY)
VALUES('user03','홍길동','1234','남자',DATE '1990-01-01','미혼',300);

실행 -> 성공

-- 오류 테스트
INSERT INTO MEMBERS(ID, NAME, PASSWORD, GENDER, BIRTH, MARRIAGE, SALARY)
VALUES('user04','홍길동','1234','외계인',DATE '1990-01-01','미혼',300);

실행 -> 오류코드(ORA-12899: value too large for column "ORAMAN"."MEMBERS"."GENDER") -> gender에 설정값 이외의 값을 넣을 수 없어서 실행불가


### # 폼 유효성 검사 (Validation Check)


# 2. 10:30 ~ 11:20
이런식으로 쭉 테스트 함

### # 오류코드 설명
SQL Error [2290] [23000]: ORA-02290: check constraint (ORAMAN.MEMBERS_PASSWORD_CK) violated

constraint(제약 조건)의 이름을 설정하지 않으면 오라클에서 SYS_C+숫자 형식으로 임의로 설정해줌
나중에 분석하기 좋게 제약 조건의 이름을 명확하게 설정하자!

### # 우리가 하는 작업은 데이터 모델링

### # UPDATE 제약조건 테스트 (UPDATE(DML) : 수정하기)
-- salary 위반
UPDATE MEMBERS SET SALARY = 50 WHERE ID='user01';
// user01의 salary를 50으로 바꿔라

-- gender 위반
UPDATE MEMBERS SET GENDER='중성' WHERE ID='user01';

-> 2개 다 실행불가 -> salary는 100부터 시작 / gender는 넣을 수 있게 설정된 값이 아님

### # A02스크립트에 게시물 테이블이라는 글 밑에 밑에 추가
-- 참조 무결성 때문에 queen 회원 추가 진행
INSERT INTO MEMBERS(ID, NAME, PASSWORD, GENDER, BIRTH, MARRIAGE, SALARY)
VALUES('queen','홍길동','1234','남자',DATE '1990-01-01','미혼',300);

### # -- 트랜잭션 테스트는 제외함
-- 예상
-- PK 중복 오류 발생

-- DELETE 테스트
DELETE FROM BOARDS WHERE NO = 2;

-- 트랜잭션 테스트
INSERT INTO BOARDS(NO, WRITER, PASSWORD, SUBJECT, CONTENT, READHIT, REGDATE)
VALUES(10, 'queen', '1234', '트랜잭션', 'rollback 테스트', 0, SYSDATE);

SELECT * FROM BOARDS;

ROLLBACK;

SELECT * FROM BOARDS;

이 부분 삭제함


### # 주석 추가

-- 경계값 테스트 (Boundary Test)
-- READHIT = 0
INSERT INTO BOARDS (NO, WRITER, PASSWORD, SUBJECT, CONTENT, READHIT, REGDATE)
VALUES(4, 'queen', '1234', '경계 테스트', '0 조회수', 0, SYSDATE);

-- READHIT = 매우 큰 값
-- 체크 제약조건이 0이상 이므로 999999999도 문제없음
INSERT INTO BOARDS(NO, WRITER, PASSWORD, SUBJECT, CONTENT, READHIT, REGDATE)
VALUES(5, 'queen', '1234', '경계 테스트', '큰 조회수', 999999999, SYSDATE);

-- NULL 허용 컬럼 테스트
-- WRITER NULL
-- 참조 무결성 제약 조건이 on delete set null이므로 WRITER 컬럼에 null 함
INSERT INTO BOARDS (NO, WRITER, PASSWORD, SUBJECT, CONTENT, READHIT, REGDATE)
VALUES(6, NULL, '1234', 'NULL 테스트', '작성자 NULL', 0, SYSDATE);

# 3. 11:30 ~ 12:20
### # 삭제
1-- 트랜잭션 테스트
INSERT INTO PRODUCTS(NUM, NAME, COMPANY, IMAGE, STOCK, PRICE, CATEGORY, CONTENT, POINT, INPUTDATE)
VALUES(20,'테스트상품','테스트','test.png',10,1000,'bread','rollback 테스트',10,SYSDATE);

SELECT * FROM PRODUCTS;

ROLLBACK;

SELECT * FROM PRODUCTS;

### # 트랜잭션 개요 (Oracle 교안 P.120~123)
개념 : 데이터를 처리하는 (업무를 처리하는) 논리적인 처리 단위
목적 : 일관성 유지(무결성 보장), 안정적 데이터 복구
특징 : ALL-OR-NOTHING 방식 - 논리적 연관 작업 그룹화가능
(commit을 하면 rollback을 못하고 / rollback을 하면 commit을 못함)

- 트랜잭션 시작과 종료
시작 : 최초 DML 문장이 실행됨과 동시에 트랜잭션 시작
종료 : commit(영구 저장) 명령어 또는 rollback(이전 작업 취소) 명령어를 사용하면 종료

** DML 문장은 마지막에 반드시 commit이나 rollback을 해야 함 ** (중요함!)
** DDL 문장은 automatic commit이여서 자동으로 commit돼서 마지막에 commit이나 rollback을 안해도 됨 ** (중요함!)

### # 트랜잭션 제어어 (Oracle 교안 P.124)
가볍게 알기
- 저장점 : 롤백해서 돌아가는 곳
형식 :
저장점 만들기 - savepoint 레이블_이름 ;
저장점으로 이동 - rollback to 레이블_이름 ;

# 4. 12:30 ~ 13:20
### # Data Modeling (DDL 실습) (Dbeaver 교안2 P.119~127)
*** A04스크립트에 아래의 내용 다 있음 ***

- 컬럼 추가
MEMBERS에 EMAIL컬럼 추가 - ALTER TABLE MEMBERS ADD EMAIL VARCHAR2(100);
PRODUCTS에 DISCOUNT컬럼 추가 - ALTER TABLE PRODUCTS ADD DISCOUNT NUMBER DEFAULT 0;

- 디자인모드로 추가해도 되지만 명령어 모드로도 할 줄 알아야 함 (A04에 있음)

- A04 맨 위에 문장 추가 - 데이터 사전 추가해서 정보 확인
-- 테이블의 컬럼 정보 확인 (*는 ALL이라는 뜻)
SELECT * FROM USER_TAB_COLUMNS WHERE TABLE_NAME = 'MEMBERS';

*** 공통 문법 : DDL TABLE 유저명.테이블명 명령어 대상 

- COLUMN_NAME만 표시하기
SELECT COLUMN_NAME FROM USER_TAB_COLUMNS WHERE TABLE_NAME = 'MEMBERS';

- PRODUCTS의 COLUMN_NAME만 표시하기
SELECT COLUMN_NAME FROM USER_TAB_COLUMNS WHERE TABLE_NAME = 'PRODUCTS';

- MEMBERS의 COLUMN중 NAME의 구조 바꾸기
ALTER TABLE ORAMAN.MEMBERS MODIFY NAME VARCHAR2(50);
구조를 수정해라 /  ORAMAN.MEMBERS를 / 수정해라 / NAME의 VARCHAR2(50)으로

- BOARDS의 COLUMN중 SUBJECT의 구조 바꾸기
ALTER TABLE ORAMAN.BOARDS MODIFY SUBJECT VARCHAR2(100);
구조를 수정해라 /  ORAMAN.BOARDS를 / 수정해라 / SUBJECT의 VARCHAR2(100)으로

- MEMBERS의 COLUMN중 ADDRESS의 이름 바꾸기
ALTER TABLE ORAMAN.MEMBERS RENAME COLUMN ADDRESS TO ADDR;
구조를 수정해라 / ORAMAN.MEMBERS를 / 이름을 바꿔라 / 컬럼 ADDRESS를 ADDR로

- PRODUCTS의 COLUMN중 CONTENT의 이름 바꾸기
ALTER TABLE ORAMAN.PRODUCTS RENAME COLUMN CONTENT TO DESCRIPTION;
구조를 수정해라 / ORAMAN.PRODUCTS를 / 이름을 바꿔라 / 컬럼 CONTENT를 DESCRIPTION로

- MEMBERS의 COLUMN중 MANAGER 삭제하기
ALTER TABLE ORAMAN.MEMBERS DROP COLUMN MANAGER;

- PRODUCTS의 COLUMN중 IMAGE 삭제하기
ALTER TABLE ORAMAN.PRODUCTS DROP COLUMN IMAGE;

- MEMBERS의 COLUMN중 SALARY 기본값 수정하기
ALTER TABLE ORAMAN.MEMBERS MODIFY SALARY NUMBER(10,2) DEFAULT 1000;

- BOARDS의 COLUMN중 READHIT 기본값 수정하기
ALTER TABLE ORAMAN.BOARDS MODIFY READHIT NUMBER DEFAULT 10;

- MEMBERS의 COLUMN중 NAME의 NOT NULL 체크하기
ALTER TABLE ORAMAN.MEMBERS MODIFY NAME VARCHAR2(50) NOT NULL;

- MEMBERS의 COLUMN중 NAME의 NOT NULL 체크해제하기
ALTER TABLE ORAMAN.MEMBERS MODIFY NAME VARCHAR2(50) NULL;

- PRODUCTS의 CONSTRAINT중 PRODUCTS_POINT_CK 삭제하기 -> 제약 조건 삭제는 DROP / 추가는 ADD
ALTER TABLE ORAMAN.PRODUCTS DROP CONSTRAINT PRODUCTS_POINT_CK;

- MEMBERS의 CONSTRAINT중 MEMBERS_PASSWORD_CK 삭제하기
ALTER TABLE ORAMAN.MEMBERS DROP CONSTRAINT MEMBERS_PASSWORD_CK;

### # 데이터 디자인 중에서 중요한 것
넣으려는 데이터 값이 제약 조건에 위배되지 않는지!
제약 조건을 바꿀 때 이미 들어가 있는 데이터 값에 위배되지 않는지!

# 5. 14:20 ~ 15:10
### # SQL - DQL (Data Query Language) - select
공부하기 전에 초기화 후 A03스크립트로 설정 다시 하기
1) A03_2 스크립튼 전체 블록 후 Alt + X

(Oracle 교안 P.134 ~ 195)


### # DQL (Data Query Language) (A05스크립트!!에 작성함)
-- DQL
-- select *|{[distinct] column 리스트|표현식, [alias],
-- from 테이블 1[, 테이블 2, …] 
-- [where condition] 
-- [group by column] 
-- [having column] 
-- [order by column];

밑에 식에 대괄호 안에 있는건 선택사항 -> select랑 from은 필수임
*은 ALL COLUMN이라는 뜻
-- 1. 모든 상품을 조회하세요.
-- *는 all columns를 의미합니다.
SELECT * FROM products ;
조회해라/모든컬럽들을/products테이블에서

-- 2. 상품 이름과 가격을 조회하세요.
-- 관심있는 컬럼만 콤마로 연결하여 조회
SELECT name, price FROM products ;

-- 단가, 입고일자, 재고 목록
SELECT price, inputdate, stock FROM products ;

-- 4. 상품의 카테고리를 중복 없이 조회하세요.
-- distinct : 중복된 데이터는 1개만 표시
select distinct category from products ;

select category from products ;

# 6. 15:20 ~ 16:10
### # 파생 컬럼
파생 컬럼은 실제로는 존재하지 않은 컬럼이지만
이 조회에서만 보여주는 컬럼
예상도? 느낌

-- 파생 컬럼과 별칭
-- 20. 상품 이름과 가격, 가격 + 1000을 조회하세요.
select name, price, price + 1000 from products ;
-> name, price, price+1000 컬럼 3개가 나오는데 price+1000컬럼이 파생 컬럼이고 실제로 존재하지 않는 컬럼임

-- 21. 상품 이름과 연간 가격(가격 * 12)을 조회하세요.
select name, price * 12 as annual_price from products ;
(as)는 있으나 없으나 임
-> select name, price * 12 annual_price from products ; 이렇게 해도 똑같은 값 나옴.
name컬럼 조회
price * 12라는 데이터 값들이 담긴 컬럼 annual_price라는 알리아스(Alias)(별칭, 가명)로 명명하여 컬럼 조회
알리아스에 원래 공백이 있으면 에러가 남
But 공백을 굳이 넣고 싶으면 쌍따옴표 사용하면 됨
-> select name, price * 12 annual price from products ; 에러남
-> select name, price * 12 "annual price" from products ; 실행됨

- 오라클에서 쌍따옴표 치는게 2가지 경우
알리아스 이름에 공백 넣을때


### # 문자열 결합 (||은 자바의 +라고 생각하면 됨)
-- 22. '상품명 : ○○, 가격 : ○○원' 형식으로 출력하세요.
select '상품명 : ' || name || ', 가격 : ' || price || '원' as result from products ; -> 따옴표는 문자열에 사용 ( ' ' )
-> 상품명 : 아메리카노01, 가격 : 6000원 -> 이런식으로 나옴


### # 행의 제한 (필터링) (조건 검색) [where condition] - condition에 연산자들이 나옴
ex) select * from products where num = 1 ; -> 자바에서 =는 대입 연산자, 할당한다 라는 뜻인데 여기서는 !같다!라는 뜻임
조회한다 / 모든 컬럼들을 / products 테이블에서 / num컬럼에 1인 데이터가 있는 행을
-> products 테이블에서 num 값이 1인 행(Row)들만 필터링하여, 해당 행의 모든 컬럼(Column) 데이터를 추출한다.
사용 예 : 상품 페이지 적용에 사용


### # and 연산자
SELECT * FROM products WHERE price = 3000 AND point = 6 ;
조회하라 / 모두 / products테이블의 / price 컬럼의 데이터가 300 그리고 point 컬럼의 데이터가 6인 행들의 데이터를

SELECT * FROM products WHERE price = 3000 OR point = 6 ;
조회하라 / 모두 / products테이블의 / price 컬럼의 데이터가 300 이거나 point 컬럼의 데이터가 6인 행들의 데이터를

-- 8. 가격이 3500 이상이고 4500 이하인 상품을 조회하세요.
SELECT * FROM products WHERE price >= 3500 AND price <= 4500 ;

BETWEEN구문을 이용해도 똑같은 결과 나옴 (이상, 이하의 개념이여서 양 끝쪽 숫자도 포함함)
SELECT * FROM products WHERE price BETWEEN 3500 AND 4500 ;


### # or 연산자
-- 9. 가격이 4000 또는 6000인 상품을 조회하세요.
SELECT * FROM products WHERE price = 4000 or price = 6000 ;

in 절(clause), in 키워드 사용 : or 연산자의 대체 표현 -> price가 4000 이거나 6000인 것 (or 연산이 많을때)
select * from products where price in (4000, 6000) ;

연습문제 : in 절을 사용하여 'cake' 또는 'beverage'인 항목들 조회
SELECT * FROM products WHERE category in ('cake', 'beverage') ;

* in : ~안에 포함되어 있는 *

# 7. 16:20 ~ 17:10
### # !=과 <>은 같은 의미임

포인트가 5가 아닌 상품을 조회하세요.
select * from products where point != 5 ;

select * from products where point <> 5 ;


### # 널 데이터(null) (Oracle 교안 P.156)
null값은 비교 판단할 수 없는 미지의 값 -> 관계 연산자를 사용하지 못한다.
12 > 7 : 참 / 5 > 7 : 거짓 / 5 > null : 모름 / null = null : 모름

따라서 null은 is를 사용해야 함
ex) 가격이 NULL인 상품을 조회하세요.
select * from products where price is null ;
SELECT * FROM boards WHERE writer IS NULL ;


### # like 연산자 (Oracle 교안 P.166)
like 키워드에 wildcard 문자가 사용됩니다.

wildcard 문자이용 :
_(언더라인) : 무조건 1글자
%(퍼센트) : 0개 이상의 문자열

(중요함!!!!!!!)
이름의 첫 번째 글자가 [선]인 회원 : select name from members where name like '선%' ;
이름 중에 [연]이라는 글자가 있는 회원 : select name from members where name like '%연%' ;
이름의 2 번째 글자가 [순] 인 회원 : select * from members where name like '_순%' ;
이름의 끝자리가 [후]인 회원 : select * from members where name like '%후' ;

* _(언더라인)을 몇번씩 써도 됨 *

사용법 : 연관검색어?

연습문제 : 상품명 2번째 글자가 '이'인 상품들 조회
SELECT * FROM products where name LIKE '_이%';


### # 부정어 사용하기
연산자 앞에 not을 붙이면 됨
but! is 연산자는 is뒤에 not을 넣음 -> is not null
간단한건 !=을 사용해도 됨.

연습문제 :
-- 카테고리가 'bread'가 아닌 항목
SELECT * FROM products WHERE CATEGORY NOT IN ('bread') ;
-- 게시물 작성자가 확실한 게시물
SELECT * FROM boards WHERE WRITER IS NOT NULL ;
-- 단가가 3500원 이상 5500원 이하에 포함되지 않는 상품
SELECT * FROM products WHERE price NOT BETWEEN 3500 AND 5500 ;
-- '크로와상'으로 시작하지 않는 상품들
SELECT * FROM products name NOT LIKE '크로와상%' ;


### # 행의 정렬 (Oracle 교안 P.184)
- 오름차순 : 작은 수부터 큰 수까지 (우리가 아는 순서대로?)
숫자 : 1 / 2 / 3
문자 : A / B / C
날짜 : 2009-01-23 / 2009-02-22 / 2009-04-15
null : 가장 마지막에 추출됨

- 내림차순 : 큰 수부터 작은 수까지
숫자 : 3 / 2 / 1
문자 : C / B / A
날짜 : 2009-04-15 / 2009-02-22 / 2009-01-23
null : 가장 먼저 추출됨

# 8. 17:20 ~ 18:10
### # order by 키워드 (order도 키워드, by도 키워드)
정렬을 하는 키워드 - asc(Ascending) <-> desc(Descending)
문법 :
order by 컬럼 (asc) ; - asc는 안써도 됨, 기본값이여서
order by 컬럼 desc ;

카테고리별로 정렬하고, 같은 카테고리에서는 가격이 높은 순으로 정렬하세요.
select * from products order by category asc, price desc ;
조회하라 / 모든 것 / products테이블에서 / 정렬해라 / 카테고리를 오름차순으로 / 같은 카테고리 안에서는 price를 내림차순으로 정렬

연습문제 : boards 테이블에서 작성자에 대하여 내림차순으로 조회하되 조회수가 큰것부터 출력
SELECT * FROM boards ORDER BY writer DESC, readhit DESC ;

연습문제2 : boards 테이블에서 작성자에 대하여 내림차순으로 조회하되 최신 게시물부터 먼저 보여 주세요.
방법1) SELECT * FROM boards ORDER BY writer DESC, regdate DESC ; -> regdate는 수정한 날짜가 될 수도 있어서 안전하게 NO(number)을 하는게 좋다.
방법2) SELECT * FROM boards ORDER BY writer DESC, NO DESC ;

### # 카테고리에 어떤 것들이 있는지 조회하기
SELECT DISTINCT category FROM products ORDER BY category ASC ;


### # select 구문의 기본 문형 (Oracle 교안 P.137)
기본 내용들
group함수와 having함수는 내일 수업함

### # 오늘 배운 것들의 실제 사용법 : (A05스크립트)
SELECT * FROM products ; 모든 상품 목록 조회
select * from products where num = 1 ; 상품에 대한 상세 보기 / 상품 주문 페이지
select * from products where price in (4000, 6000) ; 데이터 필터링할때 (하한값얼마 상한값얼마에서 찾기)

쇼핑몰 사이트 들어가서 SQL문장이 어떻게 이용되는지 고민해보기

### # BETWEEN구문 주의사항
SELECT * FROM products WHERE price BETWEEN 3500 AND 4500 ; 에서 변수?에 숫자말고 문자열 사용 가능
SELECT * FROM products WHERE name BETWEEN '김' AND '박' ;

### # order라는 테이블을 만들지 말자
order라는 키워드가 이미 있어서
테이블 이름은 복수형으로 만들어라

### # 내일 수업 내용
함수 (자바의 메소드)
