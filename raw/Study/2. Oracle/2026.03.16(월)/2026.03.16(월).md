# 1. 09:30 ~ 10:20
Oracle 교안 (P.2)
### # DBMS (DataBase Management System) : 데이터베이스 관리 시스템
줄여서 ~~DB라고 부름 ex) 오라클 DB(대용량 데이터에 이용)

오라클로 배우다가 spring(+Java고급과정)을 배우면 MySQL을 사용할 예정

### #SQL구문 (P.34)
SQL : Structured Query Language
데이터를 정의, 조작, 제어, 조회하기 위해 사용하는 표준 프로그래밍 언어
DCL제외하고 나머지는 알아야 함

DQL : 데이터 조회
DML : 회원가입, 상품등록
DDL : 구조나 틀 생성, 변경, 삭제
TCL : DML을 통해 조작된 결과를 확정 or 취소하는 등 작업 단위(트랜잭션)을 제어함

### # DBeaver
1. 기본세팅
데이터베이스 - 새 데이터베이스 연결 - Oracle - 다음
- Host(localhost), DataBase(xe), Username(sys), Role(SYSDBA),
Password(oracle), save password(체크), Client(XE) - Test Connection버튼 - driver다운로드 - Connected 나오면 괜찮은거임

- Host : 컴퓨터 / localhost : 내 컴퓨터
- DataBase(xe) : 데이터 베이스 이름이 xe(xe버전이다)임
- Username이랑 Password는 Oracle관리자 계정
- Role(SYSDBA) : 맡은 역할이 SYSDBA : System DataBase Administration (시스템 데이터베이스 관리자)
- save password(체크) : 자동로그인 (멀티유저 있으면 체크해제)
- Client(XE) : 
- Test Connection : 정상적으로 세팅이 된건지 확인하기

만약에 안되면 Edit DRiver에서 Default Database(xe), Default User(sys)로 바꾸고 확인


2. 생성된 아이콘 이름, 설명 바꾸기
생성된 아이콘 우클릭(Oracle모양 아이콘) - Edit Connection (F4번 단축키) - General
- Connection name : 로컬DB(관리자), Description : 관리자를 위한 데이터베이스 접속 아이콘입니다.
- 확인누르기

3. 
로컬DB(관리자) - Schemas(스키마) - HR - Tables - 
스키마 : 한 사람이 가지고 있는 정보를 통틀어서 부르는 말

보통 회사에 입사하면 상사인 데이터 베이스 관리자가 신입한테 데이터 베이스 계정을 받고 데이터 베이스에 접근 가능한 아이디를 하나 만들어줌
(회사 입사할때 회사 이메일 받는거랑 비슷한 느낌)

4.
받은 파일 : A01.관리자 사용자 생성하기.sql - 끌고와서 DBeaver에 놓기 - Active datasource(N/A) 클릭 후 로컬DB(관리자) 선택 후 Select
- Schemas 클릭 후 (변경 가능) But 아직 하지 않기

# 2. 10:30 ~ 11:20
Java에서의 클래스를 DBeaver에서는 스크립트라고 부름

### # 행 번호 표시 켜기
도구상자와 스크립트 사이에 공간에 우클릭 후 행 번호 표시 체크

### # 수업내용
Ctrl + Enter : 현재 쿼리 실행

라인번호 28 - 클릭 후 Ctrl + Enter
주석 작성법 : -- 사용
테이블 스페이스(tablespace) : 테이블이 저장되는 저장소

### # 관리자가 신입한테 데이터 베이스에 접근 가능한 아이디 만들어주는 과정
실제 현장에서는 데이터 베이스 할당량을 무제한으로 주지도 않고 권한도 이렇게 주지 않음
(라인 번호 : 30~39)
-- 사용자 계정 생성
-- 계정 생성하고 잠금을 풀어 줍니다.
create user oraman identified by oracle account unlock ;
					(아이디)						(비밀번호)  (생성)

-- 기본 테이블 저장소를 users로 변경합니다.
-- 할당량(quota)을 무제한(unlimited) 제공해 줍니다.
alter user oraman default tablespace users quota unlimited on users ;
														(유저에게 할당량을 무제한으로 줌)

-- oraman에게 접속 권한(connect)과 resource 권한을 부여합니다.
grant connect, resource to oraman ;        (아이디 비번이 있더라도 관리자가 권한을 주지 않으면 접속을 못함)
(접속 권한), (oraman에게 준다)    -> oraman에게 접속권한을 grant(수여한다)하다
(resource : 일반 사용자가 할 수 있는 모든 권한)

* 실제로 위의 코드를 타이핑 해보는 거 연습해보기

### #
Schemas클릭 - F5버튼 누르기(새로고침) - ORAMAN 파일이 생성되어있는거 확인
수업에서는 ORAMAN 계정으로 실습 할 예정 (관리자 계정은 가끔 사용)

### #
데이터베이스 - 새 데이터베이스 연결
- Host(localhost), DataBase(xe), Username(oraman), Role(Normal),
Password(oracle), save password(체크), Client(XE) - Test Connection버튼 - 완료
- xe클릭 후 F4버튼 누르기(이름, 설명 바꾸기) - Connection name : 로컬DB(오라맨), Description : hello - 확인


* Username(oraman) / Role(Normal) : oraman이라는 유저가 (Normal)일반 사용자라는 뜻 *

### #
파일 가장 왼쪽에 (*)표시가 있으면 저장을 하지 않았다는 뜻임

### # DBeaver 교안 (P.55)
조그만 쇼핑몰 만들기
쇼핑몰 만들기 위해서 필요한 목록들 (테이블 목록들)
회원, 게시물, 상품, 주문, 주문 상세

### # DBeaver 교안 (P.57)
- 용어 정리
가로줄 : 행 (Row (로우))
세로줄 : 렬 (Column (컬럼))

- 데이터 타입
문자열 - varchar2 (국제표준은 varchar인데 오라클에서만 varchar2임)
숫자 (정수, 실수) - number
날짜 - date

### # 테이블 생성
로컬DB(오라맨) - Schemas - ORAMAN - Tables - 우클릭 - Create New Table (단축키 : Alt + Insert)
- Name : MEMBERS, Tablespace : USERS, Comment : 회원을 위한 테이블입니다.

테이블 이름은 일반적으로 복수를 사용함 (테이블에 일반적으로 하나만 들어 있지 않아서)

* Alt + Insert (Java의 생성자 생성 단축키)(create하는 단축키로 주로 쓰임) *

### # Primary Key
절대로 중복되면 안되고 반드시 입력되야 하는 사항
ex) 회원가입할때 ID같은 정보

# 3. 11:30 ~ 12:20
### # MEMBERS테이블에 내용 추가 (Dbeaver 교안 PDF P.57)
id추가 : No items 부분 클릭 - Alt + Insert - Name : id, Type : VARCHAR (30), Comment : 회원의 아이디입니다.,
Keys에 Unique 체크, Primary Key, Name : MEMBERS_id_pk (테이블이름_컬럼이름_pk)

name추가 : Name : name, Type : VARCHAR2(30), Not Null 체크 안함
(Not Null : Null(데이터없음)상태를 허용하지 않는다 - 반드시 어떤 값이라도 입력해야 한다.)

// VARCHAR2() 소괄호 안에 있는 숫자는 글자 수(최대길이) (영문/숫자 기준으로 최대 20자가 들어갈 수 있다.)
// 일반적으로 이름은 9바이트면 충분 (한글은 한 글자당 3바이트 차지)

나머지들도 추가하기

- salary 추가
데이터 타입을 number(10, 2)을 만들때 Type : number / Precision : 10 / Scale : 2

### # 데이터베이스에서 문자열은 무조건 외따옴표다.
ex) '미혼'

### # 데이터 베이스에 테이블 반영하기
왼쪽 하단 Save - SQL Preview가 나옴 (여기에 나오는 문장들을 다 알아야 함) - Execute 실행하면 됨 -> 데이터베이스에 테이블이 생성됨
단축키 : Ctrl + S

### # SQL Preview (선생님이 별도로 메모장에 저장하라고 하심)
CREATE TABLE ORAMAN.MEMBERS (
	ID VARCHAR2(30) NOT NULL,
	NAME VARCHAR2(30) NULL,
	PASSWORD VARCHAR2(30) NULL,
	GENDER VARCHAR2(6) NULL,
	BIRTH DATE DEFAULT sysdate NULL,
	MARRIAGE VARCHAR2(30) DEFAULT '미혼' NOT NULL,
	SALARY NUMBER(10,2) DEFAULT 0 NULL,
	ADDRESS VARCHAR2(255) NULL,
	MANAGER VARCHAR2(50) NULL,
	CONSTRAINT "MEMBERS_id_pk" PRIMARY KEY (ID)
)
TABLESPACE USERS;
COMMENT ON TABLE ORAMAN.MEMBERS IS '회원을 위한 테이블입니다.';
COMMENT ON COLUMN ORAMAN.MEMBERS.ID IS '회원의 아이디입니다.';

### # 테이블에 tap이라고 부르는 것이 있음
MEMBERS테이블의 tap들 : Properties(테이블의 구조), Data(테이블의 데이터), 엔티티 관계도

### # A02.오라맨 스크립트.sql 끌고오기
N/A를 관리자에게 연결하지 않고 오라맨에 연결하기
4번 줄에 (-- 모든 회원 조회하기) 입력

8번 줄에 INSERT INTO members (DML구문 - insert문법요소 - 한 횡 추가)
9번 줄에 values();
()안에 값은 MEMBERS테이블의 값을 "순서대로" 넣어줘야 함.
-> 
date : 1990/12/25 : 생긴 모양이 외따옴표로 문자열이여도 date타입은 숫자 넣으면 알아서 암시적 형변환이 이루어져서 괜찮음
number : number 타입은 외따옴표 지워야함

* 문자열, 날짜는 외따옴표 / 숫자는 따옴표 X *

# 4. 12:30 ~ 13:20
### # Dbeaver 교안 (PDF p.47) - 단축키
Ctrl + Enter : 한 개의 Sql 문자 실행시키기
Alt + X : 여러 개의 Sql 문장을 한꺼번에 실행시키기
Alt + Insert : 새로운 객체(테이블, 컬럼, 시퀀스 등) 생성하기
Ctrl + F : 찾기/바꾸기 기능
F4 : View Xxx (Xxx는 Table, Column 등등)
F5 : 새로 고침

### # 칼럼 이름을 명시하여 추가하기
insert into members(id, name, password, gender, birth, marriage, salary, address, manager)
values('yoon', '윤봉길', 'abc1234', '남자', '1990/12/25', '미혼', 230, '용산', 'yusin') ;

### # 일부 컬럼만 명시하여 추가하기
- 급여(salary)를 명시하지 않으면 기본 값이 추가됩니다.
insert into members(id, name, password, gender, birth, marriage, address, manager)
values('nongae', '논개', 'abc1234', '여자', '1990/12/25', '미혼', '강남', 'soon') ;

### # 컬럼 이름을 임의의 순서대로 작성하기
insert into members(id, salary, manager, address, name, password, gender, birth, marriage)
values('shin', 215, 'kim9', '서대문', '신사임당', 'abc1234', '여자', '1990/12/25', '미혼') ;

### # 총 회원수 (count 함수 이용)
SELECT count(*) FROM members ;

### # 함수
커밋 : 영구 저장
(커밋의 반대 개념) 롤백 : rollback ;

### # 시퀀스와 테이블 (Oracle 교안 PDF P.328)
게시물에 매겨진 게시물 등록 번호가 매번 새로운 게시물이 올라올때마다 +1을 해야하는데
이러려면 전 게시물의 등록번호를 계속 기억해야하는데 이 과정이 피로감이 들어서
이를 해결해줄 것이 시퀀스.
번호표를 자동으로 만들어주는 기계 : 시퀀스

# 5. 14:20 ~ 15:10
### # (Dbeaver 교안 PDF P.69, 70)
1. 게시물 테이블 만들기
2. 상품 테이블 만들기
// P.60 보고 해도 됨

# 6. 15:20 ~ 16:10
### # 하나의 시퀀스가 하나의 테이블에 종속되는 관계는 아님 (Oracle 교안 PDF P.330)
시퀀스 이름 만들기
1) 왼쪽 파일들 목록에서 ORAMAN(사용하는 계정) 안에서 Sequences 폴더 찾기 및 생성

2) 테이블 이름이 boards면 시퀀스 이름은 board_seq

* Value : 현재값 - 0입력
* Increment : 증가값 - 1입력
Min Value : 최소값 - 1입력
* Max Value : 최종값 - 1000000입력
Cache : 미리 뽑은 값 -> 오늘 20개뽑고 5개쓰면 남은 15개는 휘발됨

생성!
-> 현재 값은 Value인데 Min Value부터 Max Value까지 Increment만큼 증가한다.

데이터 베이스에서 중복될 수 있기 때문에 Cycle 옵션 체크X // 잘모름

# 7. 16:20 ~ 17:10
### # 여기까지 하면
ORAMAN이라는 사용자가 3개의 테이블을 가지고 1개의 시퀀스를 가지고 있음

스크립트에
insert into 테이블면(컬럼명) values(컬럼값) -> 외우기

values 컬럼값이 default적으면 테이블의 컬럼 생성시에 적은 기본값을 넣게됨

(결과)
-- 시퀀스이름.nextval : 다음 번호표 뽑기(next value)
INSERT INTO boards(NO, writer, password, subject, content, readhit, regdate) 
values(board_seq.nextval, 'an', 'abc123', '자바', '너무 어려워', default, '1998/12/25');

전체 부분 블록 후 Alt + X
commit ;을 Ctrl + Enter (commit은 영구저장할때 사용하는 함수)
SELECT * FROM BOARDS ORDER BY NO DESC  ;을 Ctrl + Enter

253번줄 시퀀스 직접 치기
원래는 Sequences폴더에 우클릭으로 만들었다면
이번에는 직접 타이핑을 해서 생성하기

(실습)
CREATE SEQUENCE product_seq
START WITH 41
INCREMENT BY 1
MINVALUE 0
nocycle nocache ;

Ctrl + Enter로 실행 후 폴더클릭 후 F5번해서 새로고침

### # Dbeaver 교안 (PDF P.47 부터 공부하기 - 중간에 스크립트 나오면 넘기고 시퀀스 공부하기)
관리자용 아이콘만들기
ORAMAN용 아이콘 만들기

# 8. 17:20 ~ 18:10
### # Java 시험
