# 1. 09:30 ~ 10:20
### #  (Dbeaver 교안 P.72, 73)
해당 페이지의 테이블 생성
* 처음으로 꼭! 대문자로 이름을 입력해야 하는 컬럼이 생김

* 널 허용이 no + 코멘트에 기본 키 입니다
-> primary key 입력해야 함
양식 : 테이블명_컬럼명_pk


### # 시퀀스 생성
1) 시컨스 폴더 이용해서 생성

2) 직접 타이핑 해서 생성
A02.오라맨 스크립트.sql이라는 스크립트에 294번째 줄에 직접 작성함
create sequence order_seq start with 1 increment by 1 nocache ;
시퀀스를 만든다 / order_seq라는 이름으로 / 1부터 시작 / 1씩 증가 / 캐쉬는 없음


### #  (Dbeaver 교안 P.88)
사례로 주문 데이터와 주문 상세 데이터를 추가해보기
insert into orders(oid, mid, orderdate) values(order_seq.nextval, 'an', sysdate) ;
order테이블에 values(데이터값)를 추가한다. (추가할 데이터가 들어갈 테이블의 컬럼들) 데이터값(숫자 자동넘기기 시퀀스로 송장번호 자동 체크 + 테이블의 컬럼들에 추가할 데이터값)

select max(oid) from orders ;
현재 나의 송장번호 확인하기

insert into orderdetails(odid, oid, pnum, qty) values(orderdetail_seq.nextval, 1, 1, 10) ;
주문 상세에 데이터 값 입력

insert into orderdetails values(orderdetail_seq.nextval, 1, 2, 5) ;
주문 상세에 데이터 값 입력

# 2. 10:30 ~ 11:20
### # *의 의미
SQL 문법에서 *는 **'모든 것'**을 의미하는 와일드카드(Wildcard) 문자입니다.
정확하게는 **"이 테이블에 있는 모든 열(Column)을 다 가져와라"**라는 뜻이에요.

### #  (Dbeaver 교안 P.88 - 나머지도 실행하기)
insert into orders(oid, mid, orderdate) values(order_seq.nextval, 'soon', sysdate) ;
select max(oid) from orders ;
insert into orderdetails(odid, oid, pnum, qty) values(orderdetail_seq.nextval, 2, 1, 10) ;
insert into orderdetails values(orderdetail_seq.nextval, 2, 3, 10) ;
select * from orders ;
select * from orderdetails ;

-------------------------------------------------------------------------------------------------------------
insert into orders(oid, mid, orderdate) values(order_seq.nextval, 'an', sysdate) ;
select max(oid) from orders ;
insert into orderdetails(odid, oid, pnum, qty) values(orderdetail_seq.nextval, 3, 1, 5) ;
insert into orderdetails values(orderdetail_seq.nextval, 3, 4, 20) ;
select * from orders ;
select * from orderdetails ;

commit ;

select * from orders ;

select * from orderdetails ;


### # 내가 소유한 테이블 이름 확인
SELECT TABLE_NAME FROM user_tables;

SELECT table_name FROM user_tables;

키워드 (select같은거)는 소문자로 입력해도 알아서 대문자가 됨


### # 테이블을 만들어내는 스크립트 저장하기 (다음에 비슷한 양식 만들때 바로바로 양식?사용하려고)
ORAMAN 우클릭 후 SQL 생성 - DDL - 밑에 settings 체크 박스 둘다 체크
- Ctrl + A로 전체블록 후 Ctrl + C로 복사 후 메모장 새로 열어서 붙여넣기 후 (A03 Oraman ddl.sql)로 저장 (Oracle교육 - 2026.03.17(화)폴더에 있음)
나중에 쓰기 위해서 저장

-> 저장된 sql파일을 Dbeaver로 옮기기 (확인용) -> Active datasource를 로컬DB(오라맨) 커넥션으로 설정해주기
CREATE USER ORAMAN
-- IDENTIFIED BY <password>
;
이 부분을

-- CREATE USER ORAMAN
-- IDENTIFIED BY <password>
-- ;
이렇게 주석으로 막기


### # 테이블 삭제
DROP TABLE MEMBERS ;
내가 만든 MEMBERS 테이블을 삭제하기

- 나머지도 삭제
DROP TABLE BOARDS ;
DROP TABLE PRODUCTS ;
DROP TABLE ORDERS ;
DROP TABLE ORDERDETAILS ;

- 내가 소유한 테이블 이름 확인 (삭제된거 확인)
SELECT TABLE_NAME FROM user_tables;


### # 내가 소유한 시퀀스 목록 확인
SELECT * FROM user_sequences ;


### # 데이터 사전
오라클 데이터 베이스도 유저가 어떤 테이블, 어떤 시퀀스를 가지고 있는지 알아야 함
한마디로 데이터 베이스 관리 장부를 데이터 사전이라고 부름
문법:
user_객체명s
(객체 : DDL에 의해 바뀌는 것들 - 테이블, 시퀀스)

ex) 테이블 : user_tables
시퀀스 : user_sequences


### # 시퀀스 삭제
- 내가 소유한 시퀀스 목록 확인
SELECT * FROM user_sequences ;

- 삭제
DROP SEQUENCE BOARD_SEQ ;
DROP SEQUENCE ORDERDETAIL_SEQ ;
DROP SEQUENCE ORDER_SEQ ;
DROP SEQUENCE PRODUCT_SEQ ;


### # A03 Oraman ddl.sql 스크립트 파일 이용해서 테이블, 시퀀스 추가하기
스크립트 안에 있는 내용들이 여태 내가 만든 테이블, 시퀀스 내용들이니까
전체 블록하고 Alt + x (전체실행)을 누르면 다시 테이블이랑 시퀀스들이 만들어짐


### # 참조관계 (Dbeaver 교안 P.94)
0) 충족 요건 (참조 무결성 제약 조건)
main(부모) 테이블은 자식 테이블에서 사용중인 데이터는 삭제가 불가능합니다.
sub(자식) 테이블은 부모 테이블이 가지고 있는 값 또는 null값을 가질 수 있습니다.

1) 메인테이블과 서브테이블은 참조관계임
기본 키(pk)와 외래 키(fk)라는 연결 고리가 설정되어 있어야 함.

2)
- 메인테이블(master테이블)에 속한
참조 "되는" 컬럼(primary key(pk) - "기본 키")(foreign key에게 "참조됨")
- 서브테이블(detail테이블)에 속한
참조 "하는" 컬럼(foreign key(fk) - "외래 키")(primary key를 "참조함")

3) 무결성이 깨졌다. (무결성을 지키기 위해 참조 무결성 제약 조건을 걸어놓음)
메인테이블에 없는 primary key(인 컬럼)을 서브테이블에 foreign key로 참조하면 무결성이 깨짐.
(기본 키와 외래 키라는 연결 고리에 문제가 생김)

# 3. 11:30 ~ 12:20
### # 관계형 데이터베이스(RDB - 우리가 사용하는건 오라클RDB)
연결 고리가 있다 - 참조 관계이다 - 관계를 맺고 있다.
위의 관계를 가진 데이터베이스를 관계형 데이터 베이스라고 함


### # OOP (Object-Oriented Programming)
우리가 공부한 OOP는 Java - 객체를 사용해서 클래스들간의 관계를 이용하니까

### # (A02.오라맨 스크립트.sql) 351줄
Ctrl + Home : 가장 첫번째 위치로 이동
Ctrl + F : 찾기

-- 참조 무결성 테스트 - 이 문장 밑에
아래 문장 추가
---------------------------------------------------------------------------------------------------
-- 참조 무결성이 없는 상황
---------------------------------------------------------------------------------------------------
-- 회원 2명 가입
insert into members(id, name, password, gender, birth, marriage, salary, address, manager)
values('an', '안중근', 'abc1234', '남자', '1990/12/25', '결혼', 360, '서대문', 'kim9') ;

insert into members(id, name, password, gender, birth, marriage, address, manager)
values('queen', '선덕여왕', 'abc1234', '여자', '1990/12/25', '결혼', '용산', 'soon') ;

-- 'an' 게시물 작성
insert into boards(no, writer, password, subject, content, readhit, regdate)
values(BOARD_SEQ.nextval, 'an', 'abc123', '자바', '너무 어려워', default, '1980/12/25') ;

-- 'hello'가 게시물 작성(무결성에 위배)
insert into boards(no, writer, password, subject, content, readhit, regdate)
values(board_seq.nextval, 'hello', 'abc123', 'db', '그룹 바이', default, '1988/07/17') ; -> 참조 무결성이 깨진 상태임

-- 정보 조회
SELECT * FROM MEMBERS ;
SELECT * FROM BOARDS ;

### # 참조 무결성 제약 조건 (On Delete 옵션)(Dbeaver 교안 P.96)
On Delete 옵션 : pk데이터가 지워졌을때 하는 동작

1) On Delete set null 옵션
ex) 쇼핑몰에서 회원이 탈퇴하더라도, 게시물의 정보를 삭제하지 않고, 남겨 두는 경향이 있습니다. (Dbeaver 교안 P.98)
On Delete set null 옵션 사용 : pk데이터가 지워졌을때 fk값을 null값으로 바꾸기

2) On Delete cascade 옵션
ex) 쇼핑몰에서 고객이 특정 주문을 최소한다고 가정할 때, 주문 상세 정보는 동시에 삭제가 되어야 합니다. (Dbeaver 교안 P.105)
on Delete cascade 옵션 : pk데이터가 지워졌을때 fk값도 같이 삭제하기
(cascade = 연쇄반응)

### # 참조 무결성 제약 조건 상황에 맞는 On Delete 옵션 선택지 생각해보기 (Dbeaver 교안 P.97)
알아서 생각해보기
문법 :
fk의 제약 조건 이름
테이블명_컬럼명_fk

### # Foreign Keys 생성하기
서브테이블 더블클릭 - Properties - Foreign Keys 클릭 후 생성하기
Table : ORAMAN.BOARDS - 오라맨의 BOARDS라는 테이블이다.
Schema : ORAMAN - ORAMAN이라는 스키마.
Reference table(참조할 테이블)에서 메인테이블 선택 후 
Unique Key : MEMBERS_id_pk (Primary Key) - 참조할 pk의 유니크 키 입력
Custom Name에 서브테이블명_서브테이블의fk할컬럼명_fk 작성하기 -> boards서브테이블의 writer컬럼을 fk로 만들겠다.
Column에서 writer 선택
On Delete에서 Set Null 선택

# 4. 12:30 ~ 13:20
결과값 :
ALTER TABLE ORAMAN.BOARDS ADD CONSTRAINT BOARDS_MEMBERS_FK FOREIGN KEY (WRITER) REFERENCES ORAMAN.MEMBERS(ID) ON DELETE SET NULL;
-> 집가서 풀이해보기


### # 오류남 - 당연하게도
- 오류코드 이용
(오류나서 오류 코드 복사 후)
(정보조회 위에)(아래 문장 추가)
---------------------------------------------------------------------------------------------------
-- 참조 무결성 제약 조건 생성하기
---------------------------------------------------------------------------------------------------
-- 'hello'가 회원으로 존재하지 않아서 빚어진 문제
-- SQL Error [2298] [23000]: ORA-02298: cannot validate (ORAMAN.BOARDS_MEMBERS_FK)
-- parent keys not found
-- ORA-02298: cannot validate (ORAMAN.BOARDS_MEMBERS_FK) - parent keys not found

- 해결방법
-- 해결하려면 'hello'를 지우고 생성하면 됩니다.
DELETE FROM boards WHERE writer = 'hello' ;
COMMIT ;

- 다시 BOARDS 저장 실행
결과값 :
ALTER TABLE ORAMAN.BOARDS ADD CONSTRAINT BOARDS_MEMBERS_FK FOREIGN KEY (WRITER) REFERENCES ORAMAN.MEMBERS(ID) ON DELETE SET NULL;


### # 질문
-- 이문장은 오류 없이 실행될까요?
insert into boards(no, writer, password, subject, content, readhit, regdate)
values(board_seq.nextval, 'hello', 'abc123', 'db', '그룹 바이', default, '1988/07/17') ;

- 실행값 (오류남)
SQL Error [2291] [23000]: ORA-02291: integrity constraint (ORAMAN.BOARDS_MEMBERS_FK) violated - parent key not found
Error position: line: 386

integrity 무결성
violated 반칙

integrity constraint (ORAMAN.BOARDS_MEMBERS_FK) violated - 무결성 제약조건을 지키지 않았다.
parent key not found - 당신이 서브 테이블(BOARDS)에 넣으려고 하는 그 ID가, 메인 테이블(MEMBERS)의 PK(부모 키) 목록에 존재하지 않습니다."


### # 회원 삭제한 후 boards의 컬럼의 데이터 값 변화
---------------------------------------------------------------------------------------------------
-- 회원 탈퇴 테스트
---------------------------------------------------------------------------------------------------
select * from boards ; -> an의 게시물이 있음

delete from members where id = 'an' ; -> members에서 'an'을 삭제

select * from boards ; -> boards에서 fk값으로 on delete set null 옵션을 설정해서 boards의 an을 통한 fk값도 null로 바뀜

rollback ;

But 이거 해도 오류남 -> Commit이 최종 확정인데 이걸 DBEAVER 기본 설정에서 auto여서 rollback을 해도 delete한게 안돌려짐 (중요함!!!)
네, 맞습니다. COMMIT을 하는 순간 그 작업은 데이터베이스에 영구적으로 박제(저장)되는 것과 같아서, 안타깝게도 ROLLBACK으로는 되돌릴 수 없습니다.
->해결 : (Dbeaver교안 P.47)
DBEAVER는 Auto Commit = On이라서 이걸 Manual Commit으로 바꿔줘야함

그리고나서 
-- 회원 2명 가입
insert into members(id, name, password, gender, birth, marriage, salary, address, manager)
values('an', '안중근', 'abc1234', '남자', '1990/12/25', '결혼', 360, '서대문', 'kim9') ;

insert into members(id, name, password, gender, birth, marriage, address, manager)
values('queen', '선덕여왕', 'abc1234', '여자', '1990/12/25', '결혼', '용산', 'soon') ;

-- 'an' 게시물 작성
insert into boards(no, writer, password, subject, content, readhit, regdate)
values(BOARD_SEQ.nextval, 'an', 'abc123', '자바', '너무 어려워', default, '1980/12/25') ;

실행 후
반드시 commit ; 해야함!

다시
-- 정보 조회
SELECT * FROM MEMBERS ;

SELECT * FROM BOARDS ;

실행하면 오류없이 정상적인 값 나옴

### # 상품 삭제 테스트
(Dbeaver교안 P.97)
1) products랑 orderdetails 참조 관계 만들기 (fk만들기)
2) orders랑 orderdetails 참조 관계 만들기 (fk만들기)


### # 기본 키 추가 하는 방법 (기본 키 없어서 오류나는 사람만)
ALTER TABLE products ADD PRIMARY key("NUM") ;

# 5. 14:20 ~ 15:10
### # 상품 및 주문 데이터 추가 (A02.오라맨 스크립트.sql 416번 줄)
---------------------------------------------------------------------------------------------------
-- 상품 및 주문 데이터 추가
---------------------------------------------------------------------------------------------------
INSERT INTO ORAMAN.PRODUCTS (NUM,NAME,COMPANY,IMAGE,STOCK,PRICE,CATEGORY,CONTENT,POINT,INPUTDATE)
VALUES (1,'아메리카노01','스타벅스','americano01.png',100,6000,'beverage','아메리카노는 에스프레소에 물을 추가한 음료로 강렬한 향과 맛을 가지고 있습니다. 에스프레소의 양과 물의 비율에 따라 진하거나 가벼운 맛을 즐길 수 있습니다.',3,TO_DATE('90/12/22','RR/MM/DD'));

INSERT INTO ORAMAN.PRODUCTS (NUM,NAME,COMPANY,IMAGE,STOCK,PRICE,CATEGORY,CONTENT,POINT,INPUTDATE)
VALUES (2,'아메리카노02','메가커피','americano02.png',100,5000,'beverage','아메리카노는 에스프레소에 물을 추가한 음료입니다.',4,TO_DATE('80/07/26','RR/MM/DD'));

INSERT INTO ORAMAN.PRODUCTS (NUM,NAME,COMPANY,IMAGE,STOCK,PRICE,CATEGORY,CONTENT,POINT,INPUTDATE)
VALUES (3,'아메리카노03','컴포즈커피','americano03.png',100,4000,'beverage','아메리카노는 에스프레소에 물을 추가한 음료입니다.',5,TO_DATE('23/06/02','RR/MM/DD'));


insert into orders(oid, mid, orderdate) values(1, 'an', sysdate) ;
insert into orderdetails(odid, oid, pnum, qty) 
values(1, 1, 1, 10) ;
insert into orderdetails values(2, 1, 2, 5) ;

COMMIT ;

SELECT * FROM products ;
SELECT * FROM orders ;
SELECT * FROM orderdetails ;
---------------------------------------------------------------------------------------------------
-- 상품 삭제 테스트
---------------------------------------------------------------------------------------------------
select * from orderdetails ;

delete from products where num = 1 ;

select * from orderdetails ;

rollback ;

select * from orderdetails ;
---------------------------------------------------------------------------------------------------
-- 주문 취소 테스트
---------------------------------------------------------------------------------------------------
select * from orderdetails ;

delete from orders where oid = 1 ;

select * from orderdetails ;

rollback ;

select * from orderdetails ;
---------------------------------------------------------------------------------------------------

### # 참조 무결성 제약 조건 (Dbeaver교안 P.97)
3) members랑 orders 참조 관계 만들기 (fk만들기)
ALTER TABLE ORAMAN.ORDERS ADD CONSTRAINT ORDERS_MEMBERS_FK FOREIGN KEY (MID) REFERENCES ORAMAN.MEMBERS(ID) ON DELETE SET NULL;

---------------------------------------------------------------------------------------------------
-- 맴버 삭제 테스트
---------------------------------------------------------------------------------------------------

SELECT * FROM MEMBERS ;
SELECT * FROM orders ;

SELECT * FROM orders ;
DELETE FROM members WHERE id = 'an' ;
SELECT * FROM orders ;
ROLLBACK ;
SELECT * FROM orders ;

# 6. 15:20 ~ 16:10
### # 테이블 생성시 제약 조건 설정 (Dbeaver교안 P.97)
제약 조건은 5개 있음(Oracle교안 P.389) -> "제약 조건"을 "구조"라고 봐서 DDL의 ALTER를 이용하여 바꿈
1. not null : 컬럼에 null 값을 허용하지 않으므로, 반드시 입력해야 하는 컬럼을 의미합니다

2. unique : 중복된 데이터의 입력을 허용하지 않는 조건으로, 널 값은 고려 대상이 아닙니다.
내부적으로 인덱스(index)가 생성이 됩니다.

3. primary key : not null 조건과 unique 조건이 합쳐진 것입니다.
내부적으로 검색 기능의 향상을 위하여 인덱스(index)가 생성이 됩니다.

4. foreign key : 부모 테이블의 기본 키를 참조하는 서브 테이블의 컬럼에 부여되는 키를 의미합니다.

5. check : 저장 가능한 값의 범위 또는 조건을 지정하여 설정 값만 허용하기 위한 제약 조건입니다.
예를 들어 '급여 컬럼은 항상 0 이상입니다.', '성별 컬럼은 [남] 또는 [여]의 값 중에 하나가 입력이 될 수 있습니다.'
ex) Dbeaver교안 P.57
1) password 코멘트
비밀번호 (members_password_ck)
length(password)>=3 and length(password)<=50

2) 성별 코멘트
성별(members_gender_ck)
gender in('남자', '여자', null)

3) 결혼 코멘트
결혼 여부('결혼', '이혼', '미혼' 중 택일), 필수 입력 유도
members_marriage_ck, marriage in('결혼', '이혼', '미혼')

4) 급여
급여(members_salary_ck, salary >= 100)

### # 제약 조건 만들기 (Dbeaver 교안 P.108)
MEMBERS 테이블 안에 Constraints (제약 조건)을 보면 이미 not null이랑 primary key있음
문법 :
테이블명_컬럼명_ck

Constraints -> 생성 -> 이름 : MEMBERS_password_CHECK -> Type : CHECK
-> Expression(표현식) : length(password) >= 3 and length(password) <= 50

- 결과값
ALTER TABLE ORAMAN.MEMBERS
ADD CONSTRAINT MEMBERS_PASSWORD_CHECK CHECK (length(password) >= 3 and length(password) <= 50) ENABLE;

* not null은 not null이라고 표현 안되고 CHECK라고 표현되는데 이유는 모르겠지만
암튼 Constraints 목록 중에서 Condition을 보고 not null인지 다른 CHECK인지 확인하면 됨*

- 나머지도 하기

# 7. 16:20 ~ 17:10
### # Dbeaver교안 P.57, 69, 70 내용 제약 조건 설정하기

* 제약 조건에 위배 되는 것이 테이블에 있으면 제약 조건 생성 자체가 안됨
따라서 제약 조건을 만들기 전에 테이블부터 확인해야 함

### # SQL보기
ORAMAN 우클릭 - SQL생성 - DDL

DDL 스크립트를 복사하고
"제약 조건과 무결성 참조 제약 조건은 ALTER 구문으로 나누어서 간결하게 바꿔줘"
라고 AI한테 물어보면 만들어 줌

- 아래와 같이 만들어줌 (안봐도 됨)
-- 테이블 생성
-- MEMBERS
CREATE TABLE MEMBERS (
    ID VARCHAR2(30),
    NAME VARCHAR2(30),
    PASSWORD VARCHAR2(30),
    GENDER VARCHAR2(6),
    BIRTH DATE DEFAULT SYSDATE,
    MARRIAGE VARCHAR2(30) DEFAULT '미혼' NOT NULL,
    SALARY NUMBER(10,2) DEFAULT 0,
    ADDRESS VARCHAR2(255),
    MANAGER VARCHAR2(50)
);

-- BOARDS
CREATE TABLE BOARDS (
    NO NUMBER,
    WRITER VARCHAR2(30),
    PASSWORD VARCHAR2(50),
    SUBJECT VARCHAR2(50),
    CONTENT VARCHAR2(255),
    READHIT NUMBER DEFAULT 0,
    REGDATE DATE DEFAULT SYSDATE
);

-- PRODUCTS
CREATE TABLE PRODUCTS (
    NUM NUMBER,
    NAME VARCHAR2(50) NOT NULL,
    COMPANY VARCHAR2(50),
    IMAGE VARCHAR2(30),
    STOCK NUMBER DEFAULT 0,
    PRICE NUMBER DEFAULT 0,
    CATEGORY VARCHAR2(12),
    CONTENT VARCHAR2(300),
    POINT NUMBER DEFAULT 5,
    INPUTDATE DATE DEFAULT SYSDATE
);

-- ORDERS
CREATE TABLE ORDERS (
    OID NUMBER,
    MID VARCHAR2(10),
    ORDERDATE DATE DEFAULT SYSDATE
);

-- ORDERDETAILS
CREATE TABLE ORDERDETAILS (
    ODID NUMBER,
    OID NUMBER NOT NULL,
    PNUM NUMBER,
    QTY NUMBER DEFAULT 0
);


-- 제약 조건 (ALTER TABLE)
-- MEMBERS 제약 조건
ALTER TABLE MEMBERS ADD CONSTRAINT MEMBERS_PK PRIMARY KEY (ID);
ALTER TABLE MEMBERS ADD CONSTRAINT MEMBERS_PASSWORD_CK CHECK (LENGTH(PASSWORD) BETWEEN 3 AND 50);
ALTER TABLE MEMBERS ADD CONSTRAINT MEMBERS_GENDER_CK CHECK (GENDER IN ('남자','여자'));
ALTER TABLE MEMBERS ADD CONSTRAINT MEMBERS_MARRIAGE_CK CHECK (MARRIAGE IN ('결혼','이혼','미혼'));
ALTER TABLE MEMBERS ADD CONSTRAINT MEMBERS_SALARY_CK CHECK (SALARY >= 100);

-- BOARDS 제약 조건
ALTER TABLE BOARDS ADD CONSTRAINT BOARDS_PK PRIMARY KEY (NO);
ALTER TABLE BOARDS ADD CONSTRAINT BOARDS_READHIT_CK CHECK (READHIT >= 0);
ALTER TABLE BOARDS ADD CONSTRAINT BOARDS_MEMBERS_FK 
FOREIGN KEY (WRITER) REFERENCES MEMBERS(ID) ON DELETE SET NULL;

-- PRODUCTS 제약 조건
ALTER TABLE PRODUCTS ADD CONSTRAINT PRODUCTS_PK PRIMARY KEY (NUM);
ALTER TABLE PRODUCTS ADD CONSTRAINT PRODUCTS_STOCK_CK CHECK (STOCK >= 0);
ALTER TABLE PRODUCTS ADD CONSTRAINT PRODUCTS_PRICE_CK CHECK (PRICE >= 0);
ALTER TABLE PRODUCTS ADD CONSTRAINT PRODUCTS_CATEGORY_CK 
CHECK (CATEGORY IN ('bread','beverage','cake','macaron'));
ALTER TABLE PRODUCTS ADD CONSTRAINT PRODUCTS_POINT_CK CHECK (POINT >= 0);

-- ORDERS 제약 조건
ALTER TABLE ORDERS ADD CONSTRAINT ORDERS_PK PRIMARY KEY (OID);
ALTER TABLE ORDERS ADD CONSTRAINT ORDERS_MEMBERS_FK 
FOREIGN KEY (MID) REFERENCES MEMBERS(ID) ON DELETE SET NULL;

-- ORDERDETAILS 제약 조건
ALTER TABLE ORDERDETAILS ADD CONSTRAINT ORDERDETAILS_PK PRIMARY KEY (ODID);
ALTER TABLE ORDERDETAILS ADD CONSTRAINT ORDERDETAILS_ORDERS_FK 
FOREIGN KEY (OID) REFERENCES ORDERS(OID) ON DELETE CASCADE;
ALTER TABLE ORDERDETAILS ADD CONSTRAINT ORDERDETAILS_PRODUCTS_FK 
FOREIGN KEY (PNUM) REFERENCES PRODUCTS(NUM) ON DELETE SET NULL;


-- 시퀀스
CREATE SEQUENCE BOARD_SEQ;
CREATE SEQUENCE ORDERDETAIL_SEQ;
CREATE SEQUENCE ORDER_SEQ;
CREATE SEQUENCE PRODUCT_SEQ;


### # 다른 SQL 툴
ExErd
ErWin
Dbeaver
SqlDeveloper
Toad
Orange

# 8. 17:20 ~ 18:10
### # 메인테이블을 삭제할때
1) 참조관계 자체를 일단 끊어주면 메인테이블이든 서브테이블이든 그냥 개별로 삭제하면 되는데
2) 참조관계로 연결되어있으면 서브테이블부터 삭제를 하고나서 메인테이브를 삭제해야 한다.


### # A02 스크립트에서
DROP TABLE MEMBERS ;
DROP TABLE ORDERDETAILS ;
DROP TABLE ORDERS ;


### # 관리자가 유저가 생성한 데이터를 모두 삭제하는 법
(A01 스크립트 이용)
commit을 수동으로 바꾸기
drop user oraman cascade ; (41번 줄)
user oraman이 생성한 모든 것을 삭제 (퇴사한 사람꺼)
*오류남 - user oraman이 현재 연결중임
-> 로컬DB(오라맨) 커넥션 우클릭 -> 연결종료
다시 drop user oraman cascade ; (41번 줄) 실행 -> 삭제 됨


### # 계정 다시 권한 주기
-- 사용자 계정 생성
-- 계정 생성하고 잠금을 풀어 줍니다.
create user oraman identified by oracle account unlock ;

-- 기본 테이블 저장소를 users로 변경합니다.
-- 할당량(quota)을 무제한(unlimited) 제공해 줍니다.
alter user oraman default tablespace users quota unlimited on users ;

-- oraman에게 접속 권한(connect)과 resource 권한을 부여합니다.
grant connect, resource to oraman ;


### # 스크립트 가져오기
A03.Oraman ddl(선생님이 만든 버젼).sql을 가져오기
Active datasource를 oraman으로 설정
스크립트 내용 전부 블록 지정 후 Alt + X

### # autocommit 끄는 기능
상단 - 윈도우 - 환경 설정 - 연결 - 연결 유형 - auto-commit by default 체크 해제
