# 1. 09:30 ~ 10:20
#### \# 리눅스에 Java 설치하는 방법
1) wget 명령어
2) curl 명령어
3) 윈도우에 다운받기
\* 리눅스에 Java를 설치할때 Java 파일은 보통 tar.gz의 압축파일로 제공됨

#### \# 파일 압축해제(tar 명령어) - wget 명령어 이용 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.153))
- wget : 다른 사이트에서 내 PC에 다운한다 (설치는 아니고 단순 가져오기)

1) 예제 tar 파일의 주소
https://github.com/vim/vim/archive/refs/tags/v9.1.0000.tar.gz

2) 작업 디렉토리를 생성한 다음 해당 디렉토리로 이동
(~는 홈 디렉토리를 의미)
(-p 옵션은 중간 폴더가 없으면 자동으로 생성해주는 옵션)
mkdir -p ~/downloads/wgettest (생성)
cd ~/downloads/wgettest (이동)

3) 파일 확인
ls // 파일 없음

4) tar.gz 파일 다운로드 (wget으로 해당 사이트에서 파일 가져오기)

```bash
wget https://github.com/vim/vim/archive/refs/tags/v9.1.0000.tar.gz
```

5) 파일 다시 확인
ls # 파일 보임
-> v9.1.0000.tar.gz

6) 압축해제
(xzvf라는 4개의 스위치(옵션)를 이용해서 압축해제 - 압축파일 다룰때 가장 표준적 조합)

```bash
tar -xzvf v9.1.0000.tar.gz
```

7) 파일 다시 확인

```bash
ls
```

-> v9.1.0000.tar.gz  vim-9.1.0000

8) 압축해제 된 폴더로 이동하고 파일 보기

```bash
cd vim-9.1.0000/
ls
```

->
ci         CONTRIBUTING.md  LICENSE   nsis     READMEdir  README.txt      runtime      src    uninstall.txt  vimtutor.com
configure  Filelist         Makefile  pixmaps  README.md  README_VIM9.md  SECURITY.md  tools  vimtutor.bat


#### \# 파일 압축해제(tar 명령어) - curl 명령어 이용 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.154))
- curl :

1) 홈 디렉토리로 이동하기
cd

2) 작업 디렉토리 생성 및 이동
mkdir -p ~/downloads/curltest (생성)
cd ~/downloads/curltest (이동)

3) vim.tar.gz는 다운로드시 지정할 파일 이름
파일을 다운 받을 때 파일 이름을 마음대로 지정할 수 있음
(아래에서는 vim.tar.gz로 지정한 것)
(curl -L -o 다운로드_할_파일에_지정할_이름 다운로드_받을_주소)

```bash
curl -L -o vim.tar.gz https://github.com/vim/vim/archive/refs/tags/v9.1.0000.tar.gz
```

(-o: "화면에 뿌리지 말고 이 이름의 '파일'로 저장해!" (이름 지정 필수 / 저장!))
(-L: "주소가 바뀌어도 끝까지 쫓아가서 다운로드해!" (실제 데이터 확보용))

4) 파일 확인

```bash
ls
```

-> vim.tar.gz
(지정한 이름으로 다운 됨)

5) 압축 해제

```bash
tar -xzvf vim.tar.gz
```

6) 파일 다시 확인

```bash
ls
```

-> vim-9.1.0000  vim.tar.gz

7) 압축해제 된 폴더로 이동하고 파일 보기

```bash
cd vim-9.1.0000/
ls
```

->
ci         CONTRIBUTING.md  LICENSE   nsis     READMEdir  README.txt      runtime      src    uninstall.txt  vimtutor.com
configure  Filelist         Makefile  pixmaps  README.md  README_VIM9.md  SECURITY.md  tools  vimtutor.bat

8) 다른 방식으로 파일 확인하기
tree -L 2


#### \# 파일 압축해제(tar 명령어) - 윈도우에 다운받기 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.154))
- curl :

1) 홈 디렉토리로 이동하기
cd

2) window에서 다운로드 받은 파일을 넣을 폴더 생성 및 이동

```bash
sudo mkdir -p ~/downloads/fromwindows
cd ~/downloads/fromwindows
```

3) 파일을 복사하기 위하여 Drag And Drop 기능을 이용하
면 됩니다. 하지만 복사가 되지 않습니다. why?
~/downloads/fromwindows 이 폴더를 관리자로 (sudo로) 만들었는데
지금 broadcast인 일반 사용자로 들어와있어서 Permission이 거부됨
해소하려면 관리자가 broadcast에게 해당 폴더의 소유권을 넘겨주면 됨

4) cd

5) fromwindows 권한 및 소유권 확인

```bash
ls -al /home/broadcast/downloads
```

-> drwxr-xr-x 2 root      root      4096 Apr 27 01:02 fromwindows
(소유권이 관리자로 되어있음)

6) 소유권 이전하기

```bash
sudo chown -R broadcast:broadcast /home/broadcast/downloads/fromwindows/
```

7) 윈도우에서 파일에 대하여 Drag And Drop 기능을 사용하여 복사

8) 다시 해당 폴더로 이동

```bash
cd downloads/fromwindows/
```

9) 파일 목록을 확인

```bash
ls
```

-> vim-9.1.0000.tar.gz

10) 압축 해제

```bash
tar -xzvf vim-9.1.0000.tar.gz
```

11) 압축 해제된 파일 확인
cd vim-9.1.0000/ (이동)

```bash
ls
```

->
ci         CONTRIBUTING.md  LICENSE   nsis     READMEdir  README.txt      runtime      src    uninstall.txt  vimtutor.com
configure  Filelist         Makefile  pixmaps  README.md  README_VIM9.md  SECURITY.md  tools  vimtutor.bat



# 2. 10:30 ~ 11:20
#### \# 파일 압축 및 이동 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.156))

zip 명령어 사용하기
#### \# kbs\morning_garden와 mbc\infinite_challenge 디렉토리를
#### \# sbs\morning_wide 디렉토리로 복사합니다.
#### \# sbs\morning_wide 디렉토리내의 모든 파일을 압축 해제합니다.

1) 사용자 홈 디렉토리로 이동
cd

2) zip이라는 패키지 설치
sudo apt install -y zip # zip 설치

3) 소유권 바꾸기

```bash
sudo chown -R broadcast kbs/morning_garden
```

3.5) 압축할 파일에 쓰기 권한 넣기
압축할 파일의 상위 폴더로 이동

```bash
cd kbs
chmod u+x morning_garden/
```

cd (다시 홈 디렉토리로 이동)

4) 압축할 파일로 이동

```bash
cd kbs/morning_garden/
```

5) 파일 압축하기 (폴더)
(zip 명령어의 문법 : zip [옵션] [생성될_파일명] [압축할_대상])
zip -r morning_garden.zip . (. 콤마는 현재 위치를 말함)

6) 압축한 파일 이동시키기

```bash
mv morning_garden.zip /home/broadcast/sbs/morning_wide/
```

7) 사용자 홈 디렉토리로 이동
cd

7.5) 압축할 파일에 쓰기 권한 넣기
압축할 파일의 상위 폴더로 이동

```bash
cd mbc
chmod u+x infinite_challenge/
```

cd (다시 홈 디렉토리로 이동)

8) 압축할 파일로 이동

```bash
cd mbc/infinite_challenge/
```

9) 파일 압축하기 (폴더)
zip -r infinite_challenge.zip .

10) 압축한 파일 이동시키기

```bash
mv infinite_challenge.zip /home/broadcast/sbs/morning_wide/
```

#### \# 파일/디렉토리 삭제(rm / rm -rf 이용) (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.158~159))
1) 해당 파일 경로로 이동

```bash
cd downloads/wgettest/
```

2) 파일 확인

```bash
ls
```

-> v9.1.0000.tar.gz  vim-9.1.0000

3) 파일 삭제 (rm이용)

```bash
rm v9.1.0000.tar.gz
```

4) 파일 확인

```bash
ls
```

-> vim-9.1.0000

5) 디렉토리 삭제 (rm -rf 이용)
디렉토리는 원래 그냥 삭제가 안되는데 -rf로 강제 삭제함

```bash
rm -rf vim-9.1.0000/
```

6) 파일 확인

```bash
ls
```

아무것도 안나옴

7) wgettest 디렉토리 삭제

```bash
cd ..
rm -rf wgettest/
```

8) downloads 디렉토리 삭제
cd

```bash
rm -rf downloads/
```

9) ytn 디렉토리 삭제
cd

```bash
rm -rf ytn/
```

#### \# alias(알리아스) 설정 및 해제 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.161))
\* 알리아스 설정은 한시적이라 세션을 끄고 다시 키면 초기화됨 * (중요!!)
- 알리아스로 설정할 부분 확인하기
1) java.txt의 앞 3줄 출력하기
head -n 3 java.txt

(출력)
class
method
interface


2) java.txt의 마지막 5줄 출력하기
tail -n 5 java.txt

(출력)
break
continue
while
private
for


- 알리아스 설정하기
문법 :
alias 알리아스명='알리아스에_속할_데이터'

1) java.txt의 앞 3줄을 알리아스로 설정함
alias head3='head -n 3 java.txt'

2) java.txt의 마지막 5줄을 알리아스로 설정함
alias tail5='tail -n 5 java.txt'

- 알리아스로 출력해보기
1) head3

(출력)
class
method
interface


2) tail5

(출력)
break
continue
while
private
for


- 설정된 알리아스 목록 확인
alias
여러 알리아스 목록들이 확인됨

- MobaXTerm 접속 종료 후 다시 접속해서 알리아스 목록 확인하기
alias # 다시 확인하면 항목이 보이나요? -> 안보임

- 알리아스 해제 방법 (unalias 사용)
문법 :
unalias 해제할_알리아스명

unalias head3
unalias tail5



# 3. 11:30 ~ 12:20
#### \# alias(알리아스) .sh 파일로 관리 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.162))
(sh확장자는 일반적으로 shell이라는 뜻)
- 알리아스를 설정하는 shell 만들기
1) 사용자 홈 디렉토리로 이동
cd

2) .sh 파일을 생성 및 내용 추가
vi alias_test.sh

(내용 추가)
alias head3='head -n 3 java.txt'
alias tail5='tail -n 5 java.txt'
alias treeview='tree -L 1 /home/broadcast'

(저장하고 나가기)
:wq

3) source 명령어를 사용하여 실행
source alias_test.sh (알리아스 설정하는 shell)

4) alias_test.sh로 알리아스가 잘 설정된지 확인하기
head3 // 오류 없이 잘 동작함
tail5 // 오류 없이 잘 동작함


- kbs 디렉토리를 확인하는 명령어를 alias_test.sh에 추가 및 실행하기
1) kbsview라는 이름으로 'ls ~/kbs' 추가하기
(실행)
vi alias_test.sh

(내용 추가)
alias kbsview='ls ~/kbs'

(저장하고 나가기)
:wq

2) source 명령어를 사용하여 실행
source alias_test.sh

3) alias_test.sh로 알리아스가 잘 설정된지 확인하기
kbsview // 오류 없이 잘 동작함

#### \# MobaXTerm 접속 종료 후 다시 접속하기
kbsview # 다시 확인하면 항목이 보이나요? -> 안보임


- .bashrc 파일 수정하기
.bashrc는 리눅스에서 Bash 셸이 시작될 때 자동으로 실행되는 개인 설정 파일임
(세션이 시작되면 자동으로 실행되는 설정 파일임)

1) .bashrc 파일 열기
vi .bashrc

2) alias_test.sh 파일에 작성했던 내용을 모두 작성
2-1) 직접 넣기
vi .bashrc를 실행해서 아래의 코드를 직접 입력
alias head3='head -n 3 java.txt'
alias tail5='tail -n 5 java.txt'
alias treeview='tree -L 1 /home/broadcast'
alias kbsview='ls ~/kbs'

2-2) >>로 누적하기

```bash
cat alias_test.sh >> .bashrc
```

#### \# MobaXTerm 접속 종료 후 다시 접속해서 알리아스 확인하기
kbsview # 문제 없이 실행됨


#### \# 자바 테스트 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.164))
- 실습 순서
패키지 업데이트 -> JDK 설치 -> JDBC 드라이브 다운로드 -> JAVA 코딩
-> 컴파일 -> 실행


#### \# JDK/JDBC 드라이버 다운 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.165))
(실습 순서에서 패키지 업데이트 -> JDK 설치 -> JDBC 드라이브 다운로드까지의 내용)
1) 패키지를 최신으로 업데이트

```bash
sudo apt update
```

2) 자바 설치 : ubuntu나 Debian 계열 리눅스에서 자바 개발 키트 (JDK)를 설치하는 명령어
#### \# sudo apt install default-jdk
sudo apt install -y openjdk-17-jdk # 스프링 부트 버전에 맞도록 설치하면 됩니다.

3) 설치 이후 자바 버전을 확인

```bash
java -version
```

-> openjdk version "17.0.18" 2026-01-20
-> OpenJDK Runtime Environment (build 17.0.18+8-Ubuntu-124.04.1)
-> OpenJDK 64-Bit Server VM (build 17.0.18+8-Ubuntu-124.04.1, mixed mode, sharing)


4) MySQL JDBC 드라이버(Jar 파일)를 인터넷에서 다운로드하는 명령어

```bash
wget https://repo1.maven.org/maven2/com/mysql/mysql-connector-j/8.4.0/mysql-connector-j-8.4.0.jar
```

5) 다운로드 된 것들 확인하기

```bash
ls
```

->
aa.txt         java.bak  java.txt  mbc                          print_tree.sh  python.txt  total.txt
alias_test.sh  java.doc  kbs       mysql-connector-j-8.4.0.jar  python.bak     sbs


#### \# Java 코드 작성 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.167))
(교육 자료에서 JavaTest.java 파일을 집어 넣기)
(실습 순서에서 JAVA 코딩의 내용)
- 소스 코드
// import java.sql.Connection;
// import com.mysql.cj.jdbc.Driver;

```java
public class JavaTest {
	public static void main(String[] args) {
			System.out.println("초간단 자바 테스트입니다.");
	}
}
```

#### \# Java 코드 작성 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.168))
(실습 순서에서 컴파일 -> 실행까지의 내용)
(이 내용은 그냥 알아만 두기)
- 컴파일
1) 대문자 J로 시작하는 항목 조회

```bash
ls J*
```

2) 컴파일과 실행 : -cp 옵션은 클래스 경로(classpath)를 지정하는 옵션 (외부 라이브러리 포함 시 사용)
(이 명령을 실행하면 JavaTest.class Binary 파일이 생성됨)

```bash
javac -cp .:mysql-connector-j-8.4.0.jar JavaTest.java
```

- 실행
1) 현재 디렉토리와 MySQL 드라이버 JAR 파일을 클래스 경로에 포함시켜서 해당 클래스 파일을 실행

```bash
java -cp .:mysql-connector-j-8.4.0.jar JavaTest
```

2) 실행 결과

(출력)
-> 초간단 자바 테스트입니다.



# 4. 12:30 ~ 13:20
#### \# GitHub (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.170))
- Github 교안(실습).pdf (P.14) 그림으로 설명
WorkTree : 작업 폴더 (git_sample_01가 WorkTree임)
stage : 작업 공간
repository : 로컬 저장소

WorkTree에서 repository로 옮기는 것을 commit이라고 함
중간과정에 stage라고 있음
repository에서 원격 저장소에 옮기는 것을 push라고 함 / 가져오는 것을 pull이라고 함

- 해보기
1. 원격 저장소 생성 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.171))
내 깃허브 repository에 해당 이름의 repository를 생성함 (총 4개)
1) html 파일
git_sample_01 https://github.com/your_address/git_sample_01
https://github.com/Jesuisben/git_sample_01.git

2)
git_sample_02 https://github.com/your_address/git_sample_02
https://github.com/Jesuisben/git_sample_02.git

3) 스프링 파일
git_sample_03 https://github.com/your_address/git_sample_03
https://github.com/Jesuisben/git_sample_03.git

4)
git_sample_04 git_sample_02 예시에서 이미지와 내용만 바뀐 내용입니다.
https://github.com/your_address/git_sample_04
https://github.com/Jesuisben/git_sample_04.git



# 5. 14:20 ~ 15:10
#### \# GitHub (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.170))
- 해보기
2. 깃 샘플 파일에 Git Bash열기 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.172~175))
linux_수업_자료 - git_sample_01에서 빈공간 우클릭 후 Open Git Bash here 클릭
(Git Bash도 리눅스여서 리눅스에 맞는 단축키가 사용됨 - shift+insert가 붙여넣기)

(Git Bash 안에 작성하기)
1) Git 사용자 정보 설정
Git에서 커밋 작성자를 식별하기 위해 사용자 이름과 이메일을 전역(Global)으로 설정
--global 옵션을 사용하면 현재 사용자 계정 전체에 적용됨

```bash
git config --global user.name "bluesky"

git config --global user.email "bluesky@example.com"
```

2) Git 저장소 초기화
현재 디렉토리를 Git 저장소로 초기화 (Git 저장소로 설정함)(로컬 저장소 생성)

```bash
git init
```

3) .git 디렉토리가 생성되는 지 확인 (숨김파일로 생성됨)

4) 로컬 Git 저장소와 GitHub 원격 저장소를 연결
명령 실행 후 별도의 메시지가 출력되지 않으면 정상적으로 연결된 것임
(주소의 별칭을 origin으로 설정함) (주소는 내 깃허브 주소로 설정하기)
ex) git remote add origin https://github.com/your_address/git_sample_01
내가 적용한 코드 : git remote add origin https://github.com/Jesuisben/git_sample_01.git

5) 수정이 되었거나 추가된 모든 파일을 스테이징 영역에 등록하기 (스테이징)
(스테이징은 마치 장바구니에 담는 행위 - 결제하고 집에 가져가는건 커밋 - 냉장고에 넣는건 push)
(스테이징 문법 : git add 파일명
점(.)은 현재 디렉토리의 모든 변경 파일을 의미합니다.

```bash
git add .
```

6) 커밋 생성 : 스테이징된 변경 내용을 하나의 커밋으로 저장함 (-m ""은 업로드할때 메모) (커밋)

```bash
git commit -m "for sample_project"
```

6.5) 참고 사항
// 등록된 원격 저장소를 삭제하는 방법

```bash
git remote remove origin
```

// 현재 remote 확인

```bash
git remote -v
```

7) 원격 저장소로 푸시
로컬 저장소의 커밋을 GitHub 원격 저장소로 업로드하기
-u 옵션은 최초 한 번만 사용하고, 이후에는 git push, git pull 명령만으로 동작 가능

```bash
git push -u origin master
```

7-1) github 로그인 창이 뜨니까 로그인하기

8) 깃허브에 파일들이 잘 푸쉬되었는지 확인

8.5) 참고 사항
// 최초 푸시 이후 스테이징과 커밋을 동시에 할수도 있습니다.
git commit -am "커밋과 스테이징을 동시에 할래요."

// origin을 알고 있으므로, 다음과 같이 실행해도 문제 없읍니다.

```bash
git push
```

9) 다른 파일들도 하기
사용자 이름과 이메일은 이미 --global(전역)으로 설정해서 다시 할 필요 없음
.git 숨김파일이 없으면 무조건 초기화부터 해야 함
나머지는 똑같이 (단, 주소는 해당 주소로 바꾸기)



# 6. 15:20 ~ 16:10
#### \# 웹 서버 설치/구동 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.177))
- 종류
1) Apache
2) NginX

- 실습
구동되는 지 확인 후 내가 만든 웹 페이지(html)로 교체하기

- Apache 실습 순서
패키지 업데이트 -> Apache 설치 -> Apache 구동 -> ufw 방화벽 설정 -> 재부팅
-> Browser 확인 -> 로컬 파일 복사 -> index.html 파일 교체 -> Browser 확인

\* ufw (ubuntu fire wall)

- NginX 실습 순서
GitHub 클론 -> 패키지 업데이트 -> NginX 설치
-> NginX 구동 -> 파일 복사 -> Browser 확인


#### \# Apache WebServer 구동 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.178))
(실습 순서에서 패키지 업데이트 -> Apache 설치 -> Apache 구동까지의 내용)
1) 시스템에 설치된 모든 패키지를 최신 버전으로 업그레이드하기

```bash
sudo apt update
```

2) Apache 설치
apache2는 웹서버 Apache의 패키지 이름

```bash
sudo apt install -y apache2
```

3) systemctl은 System Control의 줄인 말로 서비스 실행/중지 등의 작업을 수행하도록 해주는 명령어
// enable은 시스템 부팅 시 자동으로 시작되도록 설정하는 명령어

```bash
sudo systemctl enable apache2
```

4) systemctl 구동시키기

```bash
sudo systemctl start apache2
```

5) systemctl 상태보기
sudo systemctl status apache2 // Active: active (running) 확인하기
확인되면 Ctrl + C를 눌러서 종료 합니다.


#### \# ufw 방화벽 설정 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.179~181))
(실습 순서에서 ufw 방화벽 설정 -> 재부팅 -> Browser 확인까지의 내용)
인스턴스 내부 방화벽으로 인하여 테스트가 잘 진행되지 않으면 차단을 해제해 주어야 함

1) 현재 방화벽 상태 확인

```bash
sudo ufw status
```

-> Status: inactive

2) 만약 방화벽이 활성화되어 있지 않다면 다음 명령어를 실행

```bash
sudo ufw enable
```

-> Command may disrupt existing ssh connections. Proceed
with operation (y|n)? y

Firewall is active and enabled on system startup

3) 다시 현재 방화벽 상태 확인
Status: active 이면서 80, 443 포트가 없으면 차단된 상태임

```bash
sudo ufw status
```

-> Status: active

3.5) 외워두면 좋은 port number (IT 관련 용어.pdf(P.43))
포트 넘버 - 서비스 - 내용

80 - HTTP - 웹 송수신
443 - HTTPS - 웹 송수신
22 - SSH - SSH 통신

4) 실습 (방화벽에서 위의 3개의 port number는 들어오게 허용하기)

```bash
sudo ufw allow 80/tcp
```

-> Rules updated
-> Rules updated (v6)

```bash
sudo ufw allow 443/tcp
```

-> Rules updated
-> Rules updated (v6)

```bash
sudo ufw allow 22/tcp
```

#### \# 다른 방법으로 sudo ufw allow ssh

sudo ufw status로 방화벽에서 허용한 port number를 볼 수 있음

5) 리부트하기 (Oracle VirtualBox가 리부트됨)

```bash
sudo reboot
```

리부트 되면 r을 누르면 다시 작동함

5.5) 재부팅 이후 MobaXTerm으로 접속이 잘 되지 않으면 다음 명령어를 추가적으로 작성

```bash
sudo ufw allow ssh # for mobaxterm (for ssh)
sudo ufw allow 80/tcp # for http
sudo ufw allow 443/tcp # for https
sudo ufw reload
```

6) 방화벽에서 허용한 port number 확인하기
sudo ufw status # ssh 목록 2개 보임

7) MobaXTerm으로 다시 접속 후 다음 2개의 항목이 active인지 확인

```bash
sudo systemctl status apache2
sudo systemctl status ssh
```

8) VirtualBox에서 80번 포트 포워딩 on

9) 브라우저 확인
자신의 ip주소를 주소창에 검색하면 Apache2 기본 페이지가 나옴
나의 ip 주소 : 192.168.0.37


#### \# 로컬 파일 복사 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.182))
(실습 순서에서 로컬 파일 복사 -> index.html 파일 교체 -> Browser 확인의 내용)
윈도우 운영체제에서 MobaXterm으로 홈페이지 파일 복사하기

1) 사용자 홈 디렉토리로 이동
cd

2) 홈페이지 디렉토리 생성 및 이동

```bash
mkdir myhomepage
cd myhomepage
```

3) 로컬 파일 복사
리눅스의 myhomepage 디렉토리에 교육 자료 my_homepage.zip 넣기

4) 리눅스에서 잘 옮겨졌는지 확인 후 압축 해제하기
ls // 확인용
unzip my_homepage.zip // 압축 해제

5) 아파치의 홈 디렉토리의 html 확인 및 백업 (192.168.0.37 주소 입력시 보이는 화면)
웹서버 아파치 패키지를 설치한 경우 기본 웹 문서 디렉터리 (DocumentRoot)는 '/var/www/html/'임

5-1) 아파치의 기본 html 존재 확인하기
ls -l /var/www/html/ # 아파치 홈 디렉토리 확인
-> -rw-r--r-- 1 root root 10671 Apr 27 06:31 index.html

5-2) 백업

```bash
sudo cp /var/www/html/index.html /var/www/html/index.bak
```

6) index.html 파일 교체
myhomepage에 있는 모든 파일을 아파치 기본 웹 문서 디렉토리에 덮어쓰기

```bash
sudo cp -r ./* /var/www/html/
```

ls -al /var/www/html/ (확인용)
#### \# total 3460
#### \# drwxr-xr-x 2 root root 4096 May 10 12:06 .
#### \# drwxr-xr-x 3 root root 4096 May 10 11:34 ..
#### \# -rw-r--r-- 1 root root 733 May 10 12:06 index.html
#### \# … 이하 중략

7) 실시간으로 바뀐 화면이 적용이 안될때 systemctl을 중단 후 텀을 두고 재시작 해주면 됨

```bash
sudo systemctl stop apache2
```

#### \# http://localhost:8100에서 접속이 안됨
-> 192.168.0.37

```bash
sudo systemctl start apache2
```

#### \# http://localhost:8100에서 접속이 잘 됨
-> 192.168.0.37

8) Browser 확인의 내용
정상적으로 바뀐 것 확인

9) 다음 실습을 위해 아파치 종료시키기

```bash
sudo systemctl stop apache2
```



# 7. 16:20 ~ 17:10
#### \# NginX 실습 순서
GitHub 클론 -> 패키지 업데이트 -> NginX 설치
-> NginX 구동 -> 파일 복사 -> Browser 확인

#### \# Nginx 구동 (Linux 실습(MobaXterm, VirtualBox, 실습).pdf(P.186~188))
(실습 순서에서 GitHub 클론 -> 패키지 업데이트 -> NginX 설치까지의 내용)

0) 패키지 업데이트

```bash
sudo apt update
```

1) NginX 설치

```bash
sudo apt install -y nginx
```

2) NginX 구동 및 상태 확인
sudo systemctl start nginx (시작)
sudo systemctl status nginx (상태 확인)

3) GitHub 클론
ex) git clone https://github.com/seoljinuk/git_sample_01.git
내가 입력한 코드 : git clone https://github.com/Jesuisben/git_sample_01.git

4) 목록 확인

```bash
ls
```

-> git_sample_01 확인하기

5)

```bash
cd git_sample_01/Public-2A/
```

ls (index.html 파일 확인)

6) 파일 복사 (아파치와 홈 html 파일의 경로가 똑같음)

```bash
sudo cp -r ./* /var/www/html/
```

7) Browser 확인
192.168.0.37 주소로 접속해보기



# 8. 17:20 ~ 18:10
#### \# 실습 해보기 (Linux 실습(Librarian).pdf (P.59~84))
