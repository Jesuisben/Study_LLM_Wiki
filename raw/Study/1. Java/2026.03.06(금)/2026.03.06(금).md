# 1. 09:30 ~ 10:20
- 무한 while 구문 (PDF p.49)

# 2. 10:30 ~ 11:20
- 입력 함수
```java
Scanner scan = new Scanner(System.in); -> 입력시 -> import java.util.Scanner; 자동 입력됨
int input = scan.nextInt(); // 정수형 데이터 입력함수
```

(실습)
```java
package ch2_control_statement;
```

```java
import java.util.Scanner;
```

```java
public class While03 {
    static void main(String[] args) {
        int answer = 40; // 컴퓨터가 생각하고 있는 숫자
```

```java
        //바코드 스캐너 scan을 준비합니다.
        Scanner scan = new Scanner(System.in);
```

```java
        while(true){ //endless while loop
            System.out.println("1부터 100사이의 정수 1개 입력하세요");
            int input = scan.nextInt(); // 사용자가 입력한 숫자
            System.out.println("입력한 숫자 : " + input);
```

```java
            if (answer == input) {//맞춘 경우
                System.out.println("정답입니다.");
                break;
```

```java
            }else if(answer > input){// 정답이 큰 경우
                System.out.println(input + "보다 큰 수입니다.");
```

```java
            }else{//정답이 작은 경우
                System.out.println(input + "보다 작은 수입니다.");
            }
```

```java
        }
    }
}
```

(출력)
1부터 100사이의 정수 1개 입력하세요
50
입력한 숫자 : 50
50보다 작은 수입니다.
1부터 100사이의 정수 1개 입력하세요
30
입력한 숫자 : 30
30보다 큰 수입니다.
1부터 100사이의 정수 1개 입력하세요
45
입력한 숫자 : 45
45보다 작은 수입니다.
1부터 100사이의 정수 1개 입력하세요
40
입력한 숫자 : 40
정답입니다.

(실습)
```java
package ch2_control_statement;
import java.util.Scanner;
```

```java
public class While04 {
    static void main(String[] args) {
        int total = 0; //총점
        double average = 0.0; //평균
        int counter = 0; //카운터 변수
```

```java
        Scanner scan = new Scanner(System.in);
```

```java
        while(true){
            System.out.println("점수를 입력해 주세요.");
            int jumsu = scan.nextInt();
```

```java
            if (jumsu<0){
                System.out.println("음수이므로 종료합니다.");
                break;
            }
            counter++;
            total += jumsu;
```

```java
        }
```

```java
        System.out.println("총합 : " + total);
        System.out.println("평균 : " + ((double)total/counter));
```

```java
    }
}
```

(출력)
점수를 입력해 주세요.
1
점수를 입력해 주세요.
-1
음수이므로 종료합니다.
총합 : 1
평균 : 1.0


# 3. 11:30 ~ 12:20
- 배열(array)
1부터가 아니고 0부터 시작함

- 배열의 3요소
이름, 타입, 요소개수

1. new 연산자 이용 (배열 요소의 개수, 값이 명확하지 않을때)
```java
// 배열 생성 양식 : 타입[] 배열이름 = new 타입[요소개수];
ex) int[] arr = new int[3];
정수형 타입의 데이터들이 배열 요소 / 배열 이름 : arr / 새로운 정수형 타입의 데이터들의 배열 요소의 개수는 3개이다.
-> 정수형 타입 데이터들을 배열 요소로 가질 수 있는 배열의 방이 3개이다.
-> 빈방으로 놔둬도 되기때문에 배열 요소의 개수와 값이 명확하지 않을때, 일단 방을 많이 만들고 배열 요소가 배열의 방의 개수보다 적으면 그냥 나머지는 빈방으로 놔둬도 됨.
```

```java
// 배열 요소를 배열에 집어 넣는 양식 : arr[배열 방 번호 숫자] = 배열 요소;
```

```java
// 요소 개수는 "배열이름.length"라는 메소드로 접근이 가능합니다.
```

(실습)
```java
package ch03_array;
```

```java
public class MyArr01 {
    static void main() {
        int x = 3;
        int y = 5;
```

```java
        // 배열 정의 : 정수 3개를 배열로 만들어 봅니다.
        // 타입[] 배열이름 = new 타입[요소개수];
        // 요소 개수는 "배열이름.length"라는 메소드로 접근이 가능합니다.
        // 방법01 : new 연산자를 사용하여 배열 정의
        int[] arr = new int[3] ; // arr은 배열 이름입니다.
```

```java
        arr[0] = x - y + 6; // arr의 0번째 방에 4가 들어감 // 4
        arr[2] = arr[0] + 3; // 7
        arr[1] = arr[0] + arr[2]; // 11
```


```java
        for (int i = 0; i < arr.length; i++) {
            System.out.println(arr[i]);
        }
    }
}
```

(출력)
4
11
7


2. 초기화 기법 (요소의 개수와 값이 명확할때 사용)
```java
배열 생성 양식 : 타입[] 배열이름 = {요소, 요소, 요소};
```


(실습)
```java
 // 방법02 : 초기화 기법
int[] brr = {15,30,22};
```

```java
for (int i = 0; i < brr.length; i++){
	System.out.println(brr[i]);
}
```

(출력)
15
30
22


# 4. 12:30 ~ 13:20
- 배열 생성하는 방법 2가지
PDF p.52

(실습)
```java
package ch03_array;
```

```java
public class Array01 {
    static void main(String[] args) {
        // bts 맴버들을 new 연산자를 사용하여 풀어 보세요.
        String[] bts = new String[7];
        bts[0] ="진";
        bts[1] ="뷔";
        bts[2] ="정국";
        bts[3] ="rm";
        bts[4] ="지민";
        bts[5] ="슈가";
        bts[6] ="제이홉";
```

```java
        System.out.println("배열 요소 출력하기");
        for (int i = 0; i < bts.length; i++) {
            System.out.println(bts[i]);
```

```java
        }
```

```java
    }
}
```

(출력)
배열 요소 출력하기
진
뷔
정국
rm
지민
슈가
제이홉


2. 초기화 기법

(실습)
```java
package ch03_array;
```

```java
public class Array02 {
    static void main() {
        // bts 맴버들을 초기화 기법으로 풀어 보세요. // 요소의 값이 명확하고 변동사항이 없을때 사용하는 기법
        String[] bts = {"진", "뷔", "정국", "rm", "지민", "슈가", "제이홉"};
```

```java
        System.out.println("배열 요소 출력하기");
        for (int i = 0; i < bts.length; i++) {
            System.out.println(bts[i]);
        }
    }
}
```

(출력)
배열 요소 출력하기
진
뷔
정국
rm
지민
슈가
제이홉


# 5. 14:20 ~ 15:10
배열 문제 - 입력함수, if제어문 이용

문자열형 데이터 입력 메소드 : scan.next

(실습)
```java
package ch03_array;
```

```java
import java.util.Scanner; //입력함수 불러옴
```

```java
public class EvenOdd {
    static void main() {
        Scanner scan = new Scanner(System.in); //입력함수 불러오기
        System.out.print("배열 요소 개수 입력 : "); //출력
        int size = scan.nextInt(); //입력
```

```java
        int[] jumsu = new int[size]; //배열 생성 (new 연산자 이용)
```

```java
        int odd = 0, even = 0; // 합산 변수들은 반복하기 전에 정의
```

```java
        for (int i = 0; i < jumsu.length; i++) { //jumsu.length대신 size입력해도 됨
            System.out.print(i + "번째 정수 입력 : "); //출력
            jumsu[i] = scan.nextInt(); //배열에 요소 추가
```

```java
            if (jumsu[i]%2==1){ //요소의 값이 홀수이면
                odd += jumsu[i];
            }else{ //아니면 (요소의 값이 짝수이면)
                even += jumsu[i];
            }
        }
```

```java
        System.out.println("홀수의 총합 : " + odd);
        System.out.println("짝수의 총합 : " + even);
    }
}
```

(출력)
배열 요소 개수 입력 : 4
0번째 정수 입력 : 5
1번째 정수 입력 : 3
2번째 정수 입력 : 4
3번째 정수 입력 : 8
홀수의 총합 : 8
짝수의 총합 : 12

# 6. 15:20 ~ 16:10

(실습)
```java
package ch03_array;
```

/*
학생의 이름과 국어, 영어, 수학 점수를 입력 받고 학새에 대한 총점과 평균을 출력해 주는 프로그램을 작성해 보세요.
국어, 영어, 수학 점수는 반드시 정수형 배열을 이용하여야 합니다.
*/

/*
출력 결과

이름 입력 : 김철수
    점수 입력 : 60
    점수 입력 : 40
    점수 입력 : 70

학생의 정보
이름 : 김철수, 총점 : 170.00, 평균 : 56.67
*/


```java
import java.util.Scanner;
```

```java
public class StudentExam {
    static void main() {
        Scanner scan = new Scanner(System.in);
```

```java
        System.out.print("이름 입력 : ");
        String name = scan.nextLine();
```

```java
        //int[] score = new int[3];
        String[] subject = {"국어", "영어", "수학"};
```

```java
        double total = 0.0;
        double average = 0.0;
```


```java
        for(int i = 0; i<subject.length; i++){
            System.out.print(subject[i] + " 점수 입력 : ");
            int input = scan.nextInt();
```

```java
            total += input;
        }
```

```java
        average = total/subject.length;
        System.out.println();
        System.out.println("학생의 정보");
        System.out.printf("이름 : %s, 총점 : %.2f, 평균 : %.2f", name, total, average);
```


```java
    }
}
```


(출력)
이름 입력 : 김철수
국어 점수 입력 : 60
영어 점수 입력 : 40
수학 점수 입력 : 70

학생의 정보
이름 : 김철수, 총점 : 170.00, 평균 : 56.67

# 7. 16:20 ~ 17:10
숙제 설명

# 8. 17:20 ~ 18:10
자리변경
