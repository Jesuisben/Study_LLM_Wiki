# 1. 09:30 ~ 10:20
# 패키지 안에 패키지(하위 패키지)만들기 가능
생성 : ch05_inheritance패키지 우클릭 -> 패키지 생성 -> general

# 상속
문법 :
```java
public class 서브클래스 extends 수퍼클래스 {
}
```

```java
public class Americano03 extends Beverage03 {
}
```

# 일반화
보편적인 공통분모를 수퍼클래스에 적는 것 -> 서브클래스들이 수퍼클래스에서 상속받을 수 있도록
(Beverage03 - 수퍼클래스)
```java
package ch05_inheritance.general;
```

```java
public class Beverage03 {
    private String name; // 음료 이름
    private double price; // 단가
}
```

# 클래스 디자인
클래스에 내용을 추가해서 완성하는 것

# private 맴버 변수
클래스 외부에 사용 불가

# 2. 10:30 ~ 11:20
# private 맴버 변수를 다른 클래스에서 사용하려면
/*
서브 클래스가 수퍼 클래스의 변수에 접근
1. private은 getter/setter로 접근
2. private 대신 protected로 작성
3. 생성자를 통한 대입
*/


# 3. 생성자를 통한 대입 사용
```java
// 서브 클래스의 생성자의 첫줄에는 super(); 라는 단어가 숨어있다.
// super(); - 매개변수가 0개인 super클래스의 생성자
ex) Americano03클래스의 생성자 public Americano03(String name, double price, double waterAmount){...}의 첫줄에는 super();라는 단어가 숨어있다.
// 사용자가 super(매개변수); 형식으로 다시 작성하면, super();는 사라집니다. - 마치 클래스의 타입에 기본값을 다시 작성하면 사라지는 것 처럼
```

```java
super();의 매개변수에 private때문에 사용하지 못하는 변수를 넣고 다시 수퍼클래스에 보내면 수퍼클래스 내에서는 private변수를 사용할 수 있으니까
이렇게 사용할 수 있음
그런데 수퍼클래스에 다시 보낼때 수퍼클래스에서 이 super(매개변수);를 받을 수 있게 1대1 대응이 가능한 생성자를 만들어야 함
```

(실습)
(main클래스)
```java
package ch05_inheritance.general;
```

```java
public class InheTest03 {
    static void main(String[] args) {
		// 1) Americano03클래스의 Americano03생성자로 값이 넘어감
        Americano03 americano = new Americano03("아메리카노", 4000.0, 200.0);
        Espresso03 espresso = new Espresso03();
        Latte03 latte = new Latte03();
    }
}
```

(Americano03클래스)
```java
package ch05_inheritance.general;
```

```java
public class Americano03 extends Beverage03 {
    private double waterAmount; // 투입하는 물의 양
```

```java
    // 생성자 만들기 (메소드와 똑같음)
	// 2) main클래스의 Americano03생성자으로부터 값을 받아서 현재 String name은
	// "아메리카노"double price는 4000.0 / double waterAmount는 200.0 임
    public Americano03(String name, double price, double waterAmount){
```

```java
        // 서브 클래스의 생성자의 첫줄에는 super(); 라는 단어가 숨어있다.
        // super(); - 매개변수가 0개인 super클래스의 생성자
        // 사용자가 super(매개변수); 형식으로 다시 작성하면, super();는 사라집니다.
```

```java
		// 4) public Americano03의 매개변수에 name과 price 변수를 사용하기 위한 껍데기를 줘야하는데
		// super클래스에서 private이여서 가져와야해서 super클래스에 보낼 생성자를 사용
        super(name, price);
```

```java
		// 3) double waterAmount라는 데이터를 쓰기 위한 정의된 껍데기가 필요해서 Americano03클래스의 waterAmount 맴버 변수 사용
        this.waterAmount = waterAmount;
    }
}
```

(Beverage03클래스)
```java
public class Beverage03 {
    private String name; // 음료 이름
    private double price; // 단가
```

```java
	// 5) 4에서 보내준 super(name, price);의 생성자와 1대1 대응되는 생성자를 만듬
    public Beverage03(String name, double price){
		// 6)서브 클래스에 맴버 변수 껍데기를 보내기 위해 재정의
        this.name = name;
        this.price = price;
    }
}
```

```java
// 7) 다시 역 순서로 데이터를 보냄
```

# (# 3. 생성자를 통한 대입 사용) 이 방법을 사용해서 생긴 오류 잡기
```java
이 방법을 사용하면 같은 수퍼클래스를 공유하고 있는 다른 서브 클래스에는 똑같이 super();이 숨겨져 있는데
super();이 똑같이 있다가 갑자기
```

```java
public Beverage03(String name, double price){
	this.name = name;
	this.price = price;
}
```

이걸로 교체되어서 오류가 생김

```java
그래서 수퍼클래스에 super();와 대응할
public Beverage03(){}
이것을 만들어주면 해결됨
```

# private으로 사용하지 못하는 수퍼클래스의 맴버 변수 사용하기 2번째 방법
수퍼클래스에 protected 혹은 public 메소드를 만들어서 그 안에 실행될 구문에 수퍼클래스의 맴버 변수를 사용하고
이렇게 만들어진 protected 혹은 public 메소드를 서브 클래스에 보내면 됨
```java
보낼때는 super라는 객체를 이용해서 super.수퍼클래스에서만든메소드명();
```

(실습)
(수퍼클래스)
```java
protected void showInfo(){
	System.out.println("이름 : " + name);
	System.out.println("가격 : " + price);
}
```

(서브클래스)
```java
public void printInfo(){
    super.showInfo();
    System.out.println("투입된 물의 량 : " + waterAmount + "ml");
}
```

(main클래스)
```java
americano.printInfo();
```

(출력)
이름 : 아메리카노
가격 : 4000.0
투입된 물의 량 : 200.0ml

# 3. 11:30 ~ 12:20
나머지 서브 클래스들도 똑같이 만들기
내용은
Americano03 / Espresso03 / Latte03 / Beverage03 / InheTest03 클래스들에 있음

# 4. 12:30 ~ 13:20
Americano04 / Espresso04 / Latte04 / Beverage04 / InheTest04 클래스들 생성

# 참조 형변환

1) 승급(UpCasting)
두 클래스가 상속관계라는 전제 하에서
낮은 등급의 클래스가 높은 등급의 클래스 타입으로 한시적 형태가 바뀌는 동작
```java
package ch05_inheritance.mybeverage;
```

```java
public class InheTest04 {
	static void main(String[] args) {
		Americano04 beverage01 = new Americano04("아메리카노", 4000.0, 250.0);
		beverage01.showInfo();
	}
}
```

	(Americano04를 Beverage04로 바꾸기)

```java
package ch05_inheritance.mybeverage;
```

```java
public class InheTest04 {
	static void main(String[] args) {
		// Beverage04로 바꿔도 오류가 뜨지 않음
		Beverage04 beverage01 = new Americano04("아메리카노", 4000.0, 250.0);
		beverage01.showInfo();
	}
}
```

(실습)
```java
package ch05_inheritance.mybeverage;
```

```java
public class InheTest04 {
	static void main(String[] args) {
		// 승급(UpCasting) : 낮은 등급의 클래스가 높은 등급의 클래스 타입으로 한시적 형태가 바뀌는 동작
		// 참조 형변환
		// 전제 조건 : 반드시 상속 관계이어야 합니다.
		// 부모입장에서 자식들 관리를 편하게 하려고 사용하는 방법
		Beverage04 beverage01 = new Americano04("아메리카노", 4000.0, 250.0);
		// beverage01은 승급(UpCasting)에 의해 Americano04에서 Beverage04의 객체가 완전히 바뀐것처럼 보이지만
		// 절대 아님! 주의해야함! - 한!시!적!으로 형태가 바뀌는거지 본질이 바뀐건 아님
		// But beverage01객체는 Americano04클래스의 메소드를 못쓰고 Beverage04클래스의 메소드밖에 못씀
		// 암시적 형변환
```

```java
		// 수퍼 클래스내 들어 있는 메소드는 상속의 개념에 의하여 접근이 가능합니다.
		beverage01.showInfo();
	}
}
```

(출력)
음료 이름 : 아메리카노
단가 : 4000.0원

2) 강등(DownCasting)
```java
Americano04 cofeee = (Americano04)beverage01; // 명시적 형변환
//이해하기 위해서는
// int i = (int)3.14; 처럼 생각해서 클래스도 개발자가 만들어 낸 타입이라고 생각해서 그대로 대입
// 클래스명(타입) 새로운객체(변수) = (클래스명(타입))이전객체명(데이터값);
```

```java
cofeee.sipAmericano(); // 이제 다시 cofeee가 Americano04의 객체가 되어서 Americano04의 메소드를 사용할 수 있음
```

# 5. 14:20 ~ 15:10
alt + insert키 : 생성 단축키

# 강등(DownCasting) 해보기
(main클래스)
```java
Beverage04 beverage02 = new Espresso04("에스프레소", 5000.0, 2);
beverage02.showInfo();
```

```java
Espresso04 espreeso = (Espresso04)beverage02;
espreeso.drinkEspresso();
```

(Espresso04클래스)
```java
public void drinkEspresso(){
	String message = "투입할 샷의 개수는" + shotCount + "개입니다.";
	System.out.println(message);
}
```

# 6. 15:20 ~ 16:10
# 승급 개념과 배열의 사용
```java
//클래스(타입처럼 써서) 배열 생성
Beverage04[] beverage = {
		beverage01,
		new Espresso04("마이뿌레소", 2000.0, 1),
		new Latte04("바나나 라떼",7000.0,"바나나 우유")
```

```java
};
```

```java
for (int i = 0; i < beverage.length; i++) {
	System.out.println("--------------------------------------------");
	beverage[i].showInfo();
}
```

(출력)
--------------------------------------------
음료 이름 : 아메리카노
단가 : 4000.0원
--------------------------------------------
음료 이름 : 마이뿌레소
단가 : 2000.0원
--------------------------------------------
음료 이름 : 바나나 라떼
단가 : 7000.0원

# 배열, 반복문 + 선택문 이용
```java
// instanceof 연산자 : 승급된 객체 변수가 수많은 서브 클래스 중에서 어떤 클래스로
//        		   				   생성이 되었는 지를 판단하고자 할 때 사용합니다.
-> 처음에 생성된 곳!이 어디니?
-> 너 고향이 어디니?같은 느낌인가봄
```

문법 :
객체 instanceof 클래스

결과값 : 논리타입
true, false

ex) beverage[i] instanceof Americano04 -> beverage[i]가 Americano04클래스에 들어 있는거 맞니?

(실습)
```java
for (int i = 0; i < beverage.length; i++) {
System.out.println("--------------------------------------------");
beverage[i].showInfo();
```

```java
// instanceof : 승급된 객체 변수가 수많은 서브 클래스 중에서 어떤 클래스로
//              생성이 되었는 지를 판단하고자 할 때 사용합니다.
if(beverage[i] instanceof Americano04){ // 아메리카노
	Americano04 ameri = (Americano04)beverage[i];
	ameri.sipAmericano();
```

```java
}else if(beverage[i] instanceof Espresso04){ // 에스프레소
	Espresso04 espre = (Espresso04)beverage[i];
	espre.drinkEspresso();
```

```java
}else if(beverage[i] instanceof Latte04){ // 라떼
	Latte04 latt = (Latte04)beverage[i];
	latt.enjoyLatte();
}else{ // 차후에...
```

```java
}
```

(출력)
--------------------------------------------
음료 이름 : 아메리카노
단가 : 4000.0원
아메리카노를 홀짝 홀짝 마십니다.
--------------------------------------------
음료 이름 : 마이뿌레소
단가 : 2000.0원
투입할 샷의 개수는1개입니다.
--------------------------------------------
음료 이름 : 바나나 라떼
단가 : 7000.0원
부드럽고 크리미한 바나나 라떼를 마십니다.

# 7. 16:20 ~ 17:10
```java
// 객체를 프린트하면 암시적으로 toString() 메소드가 호출이 됩니다.
System.out.println(beverage[i]); //System.out.println(beverage01.toString);
```

(Beverage04클래스)
@Override
```java
public String toString(){
	String imsi = "상품명 : " + name + ", 단가 : " + price;
	return imsi;
}
```

(출력)
상품명 : 바나나 라떼, 단가 : 7000.0

- toString도 표현하려면 이렇게 하면 됨
@Override
```java
public String toString(){
	String imsi = "상품명 : " + name + ", 단가 : " + price;
	return super.toString() + "\n" + imsi;
}
```

# 줄바꿈 키워드
\n

# 같은 메소드를 연속적으로 오버라이딩하기 (오브젝트 -> 수퍼클래스 -> 서브클래스)(toString이용)
(Beverage04)(수퍼클래스)(오브젝트의 서브클래스)
@Override // 재정의
```java
public String toString(){
	String imsi = "상품명 : " + name + ", 단가 : " + price;
	// return super.toString() + "\n" + imsi;
	return imsi;
}
```

(Americano04)(서브클래스)
@Override
```java
public String toString() {
	String imsi = ". 물의 양 : " + waterAmount;
	return super.toString() + imsi;
}
```

(main클래스)
```java
for (int i = 0; i < beverage.length; i++) {
	System.out.println("--------------------------------------------");
	beverage[i].showInfo();
```

```java
	// instanceof : 승급된 객체 변수가 수많은 서브 클래스 중에서 어떤 클래스로
	//              생성이 되었는 지를 판단하고자 할 때 사용합니다.
	if(beverage[i] instanceof Americano04){ // 아메리카노
		Americano04 ameri = (Americano04)beverage[i];
		ameri.sipAmericano();
		// 객체를 프린트하면 암시적으로 toString() 메소드가 호출이 됩니다.
		System.out.println(beverage[i]);
		//System.out.println(beverage01.toString);
}
```

(출력)
음료 이름 : 아메리카노
단가 : 4000.0원
아메리카노를 홀짝 홀짝 마십니다.
상품명 : 아메리카노, 단가 : 4000.0, 물의 양 : 250.0

# 8. 17:20 ~ 18:10
# 나머지 클래스들도 모두 오버라이딩하기

(Beverage04클래스)
@Override
```java
public String toString(){
	String imsi = "상품명 : " + name + ", 단가 : " + price;
	// return super.toString() + "\n" + imsi;
	return imsi;
}
```

(Americano04클래스)
@Override
```java
public String toString() {
	String imsi = ", 물의 양 : " + waterAmount;
	return super.toString() + imsi;
}
```

(Espresso04클래스)
@Override
```java
public String toString() {
	String imsi = ", 샷 추가 갯수 : " + shotCount;
	return super.toString() + imsi;
}
```

(Latte04클래스)
@Override
```java
public String toString() {
	String imsi = ", 우유의 타입 : " + milkType;
	return super.toString() + imsi;
}
```

(main클래스)
```java
for (int i = 0; i < beverage.length; i++) {
	System.out.println("--------------------------------------------");
	beverage[i].showInfo();
```

```java
	// instanceof : 승급된 객체 변수가 수많은 서브 클래스 중에서 어떤 클래스로
	//              생성이 되었는 지를 판단하고자 할 때 사용합니다.
	if(beverage[i] instanceof Americano04){ // 아메리카노
		Americano04 ameri = (Americano04)beverage[i];
		ameri.sipAmericano();
		// 객체를 프린트하면 암시적으로 toString() 메소드가 호출이 됩니다.
		System.out.println(beverage[i]);
		//System.out.println(beverage01.toString);
```

```java
	}else if(beverage[i] instanceof Espresso04){ // 에스프레소
		Espresso04 espre = (Espresso04)beverage[i];
		espre.drinkEspresso();
		System.out.println(beverage[i]);
```

```java
	}else if(beverage[i] instanceof Latte04){ // 라떼
		Latte04 latt = (Latte04)beverage[i];
		latt.enjoyLatte();
		System.out.println(beverage[i]);
	}else{ // 차후에...
```

```java
	}
}
```
