# 1. 09:30 ~ 10:20
# 클래스 내에 기본 생성자가 있는데 눈에는 보이지 않음
# 생성자
정의 : 객체 생성시 오직 1번만 호출이 되는 특수한 메소드
목적 : 맴버 변수들의 초기화(기본값을 넣는다)

- 생성자와 메소드의 차이점
생성자 : 오직 1번 호출 / 호출 시점이 객체 생성시
메소드 : 자유롭게 호출 / 호출 시점이 자유로움

```java
// 찾은 자료
생성자는 객체가 생성될때 초기화를 시키고 메소드는 객체가 생성된 후 동작과 기능을 부여함
생성자의 이름은 클래스 이름과 동일해야하지만 메소드는 아님
생성자는 반환 타입이 없고 메소드는 반드시 명시해야 함
생성자는 new로 딱 한 번 실행되고 메소드는 원할때 언제든 호출 가능함
```

- 생성자 : 변수들의 값을 초기화할 목적으로 사용하는 메소드
- 기본 생성자를 줬지만 안보이니까 내가 컨트롤 할 수 없으니까 개발자가 필요하면 따로 만들 수 있다.
* 개발자가 생성자를 만들면! 눈에 보이지 않는 기본 생성자는 사라진다! *
혹은 개발자가 따로 만든 생성자를 기본 생성자라고 부르기도 한다.


# 생성자 만들기 (클래스 안에서)
- 생성자도 메소드여서 메소드 만드는 문법을 똑같이 적용
```java
// but 생성자는 반환타입이 없고 원래는 반환타입이 없으면 void를 적지만 그것도 적지 않음
// 생성자의 이름은 클래스 이름과 동일
// 매개변수는 main클래스에 있는 (객체 생성시 이용한) 생성자의 소괄호 안을 보고 그걸 적음
```

문법 :
```java
반환타입 메소드이름(매개변수리스트){...}
```

```java
Product03(){
```

```java
}
```

(실습)
(클래스)
```java
package ch04_class;
```

```java
public class Product03 {
    // 클래스 생성
    // 맴버 변수 생성
    private String name ; // null
    private int price ; // 0
    private String inputdate ; // null
```

```java
    // 기본 생성자 : 눈에는 보이지 않음
    // 정의 : 객체 생성시 오직 1번만 호출이 되는 특수한 메소드
```

```java
    // 생성자 만들기
    // 반환타입 메소드이름(매개변수리스트){...}
    Product03(){
        System.out.println("하하하");
    }
```

```java
}
```

(main클래스)
```java
package ch04_class;
```

```java
public class ProductMain03 {
    static void main(String[] args) {
        // 객체 생성 (선언 및 할당)
        // 클래스명 객체명 = new연산자 생성자();
        Product03 shin = new Product03();
```

```java
    }
}
```

(출력)
하하하

# 자동만들기
(main클래스)
```java
shin.display(); // display가 빨간색으로 뜸 -> 마우스 가져다대고 create method 'display' in 'Product03' 클릭하면
```

(클래스)
```java
public void display() { // 이런 구문이 자동생성됨
```

```java
    }
```

# 2. 10:30 ~ 11:20
# main클래스와 클래스를 생성자로 연결하기 (매개변수 추가)
(main클래스)
```java
Product03 shin = new Product03("신라면", 100, "2026/03/01");
```

(클래스)
```java
// 여기에도 매개변수 개수가 맡는 생성자를 만들어줘야함
// 생성자가 클래스에 하나 더 있어도 매개변수가 같은 클래스와 main클래스의 생성자가 연결됨
```

```java
public Product03(String name, int price, String inputdate){
	this.name = name ; // 객체 자신(this)이 소유한 name 변수에 매개 변수 name의 값을 할당하시오
	this.price = price;
	this.inputdate = inputdate;
```

```java
}
```

매개변수는 그 구문이 지나면 휘발됨 그래서 클래스의 맴버 변수 name에 매개 변수 name의 값을 할당함
경로 : main클래스의 생성자의 매개변수 "신라면" -> 클래스의 생성자의 매개변수 String name -> 생성자 구문 안에서 (this.name이용해서 할당)
-> 클래스의 맴버 변수 String name

this키워드 사용하는 이유
클래스 내부에 String name이 있을때 이 맴버 변수 String name을 생성자 내부에 가져오고 싶을때
그냥 바로 String name을 생성자 내부에 작성하면 새로운 String name이라는 변수를 선언한걸로 인식하기때문에
클래스 내부의 맴버 변수 String name을 가져올때는 this라는 키워드를 이용해서
this.name이라고 해야 클래스 내부의 맴버 변수 String name으로 인식함

# 오버로딩
오버로딩이란 무엇인가 ? (=다중 정의)
한 클래스 안에 이름은 같지만 매개변수(개수나 타입)가 다른 메소드나 생성자를 여러 개
만드는 것
이름 하나에 여러 기능을 얹는 것

목적 : 여러가지 이름의 메소드를 따로따로 만들면 만들기도 번거롭고 제3자가 봤을때 헷갈리니까
ex) 더하기는 add라는 메소드로 통일하자!

조건 : 메소드 이름 동일 / 매개변수의 개수 or 타입이 달라야 함

```java
// 예시
```
(main클래스)
```java
Product03 shin = new Product03("신라면", 100, "2026/03/01");
```

(클래스)
```java
public Product03(){
	System.out.println("하하하");
}
```

```java
public Product03(String name, int price, String inputdate){
	this.name = name ; // 객체 자신(this)이 소유한 name 변수에 매개 변수 name의 값을 할당하시오
	this.price = price;
	this.inputdate = inputdate;
```

```java
}
```

# 맴버 변수의 기본값 설정
맴버 변수 생성하고 값을 할당
```java
private int price = 500 ;
```

# 생성자의 매개 변수의 이름 설정
(main클래스)
```java
Product03 blackbean = new Product03("짜파게티", "2026/03/10");
```

(클래스)
```java
public Product03 (String name, String inputdate){
	this.name = name;
	this.inputdate = inputdate;
}
```

- 사실상 String으로 매치가 되기 때문에 클래스 내부의 생성자 내부의 매개변수의 이름은!(타입X) 마음대로 정해도 됨
- 결국 해당 생성자 내부 구문에서만 이용하는 이름이라서 상관없음

# 기본값 적용해보기
(main클래스)
```java
Product03 blackbean = new Product03("짜파게티", "2026/03/10");
blackbean.display();
```

(클래스)
```java
private String name ; // null
// 상품의 단가가 입력되지 않으면 500원이라고 가정합니다.
// 기본값 재할당 (원래 기본값이 0인데 내가 임의로 지정함)
private int price = 500 ; // 0
private String inputdate ;
```

```java
public Product03 (String name, String inputdate){
	this.name = name;
	this.price = price;
	this.inputdate = inputdate;
}
```

```java
public void display() {
	System.out.println("상품명 : " + name);
	System.out.println("단가 : " + price);
	System.out.println("입고 : " + inputdate);
}
```

(출력)
상품명 : 짜파게티
단가 : 500
입고 : 2026/03/10

# 3. 11:30 ~ 12:20
#배열에 객체 이용 (new 연산자 기법)
(main클래스)
```java
package ch04_class;
```

```java
public class ProductMain03 {
    static void main(String[] args) {
        // 객체 생성 (선언 및 할당)
        // 클래스명 객체명 = new연산자 생성자();
        Product03 shin = new Product03("신라면", 1000, "2026/03/01");
        shin.display();
```

```java
        Product03 blackbean = new Product03("짜파게티", "2026/03/10");
        blackbean.display();
```

```java
        Product03 banana = new Product03();
        banana.display();
```

```java
        int size = 2; // 상품 2개
        System.out.println("상품 " + size + "를 배열로 다뤄 봅니다.");
```

```java
        int[]arr = new int[3]; //타입[]배열명 = new연산자 타입[요소개수];
```

```java
        //클래스도 내가 만든 "타입"이라서 배열 만들기 가능 // 생성자들의 배열
        // 배열 정의 //객체인 상품 2개 있다~
        Product03[] itemlist = new Product03[size];
```

```java
        // 0번째 객체 생성 *객체 생성을 이렇게도 할 수 있구나 (new 생성자())
        // 배열 객체 생성 문법 : 배열명[배열요소개수] = new연산자 클래스명(매개변수);
        itemlist[0] = new Product03("소이조이", 2000, "2025/08/15");
        itemlist[1] = new Product03("맥심커피", "2025/07/17");
```

```java
        for (int i = 0; i < itemlist.length; i++) {
            System.out.println((i+1) + "번째 상품 정보");
            itemlist[i].display();
        }
    }
}
```

(클래스)
```java
package ch04_class;
```

```java
public class Product03 {
    // 클래스 생성
    // 맴버 변수 생성
    private String name ; // null
```

```java
    // 상품의 단가가 입력되지 않으면 500원이라고 가정합니다.
    private int price = 500 ; // 0
```

```java
    private String inputdate ; // null
```

```java
    // 기본 생성자 : 눈에는 보이지 않음
    // 정의 : 객체 생성시 오직 1번만 호출이 되는 특수한 메소드
```

```java
    // 생성자 만들기
    // 반환타입 메소드이름(매개변수리스트){...}
    public Product03(){
        System.out.println("하하하");
    }
```

```java
    public Product03 (String name, String inputdate){
        this.name = name;
        this.price = price;
        this.inputdate = inputdate;
    }
```

```java
    public Product03(String name, int price, String inputdate){
        this.name = name; // 객체 자신(this)이 소유한 name 변수에 매개 변수 name의 값을 할당하시오
        this.price = price;
        this.inputdate = inputdate;
    }
```

```java
    public void display() {
        System.out.println("상품명 : " + name);
        System.out.println("단가 : " + price);
        System.out.println("입고 : " + inputdate);
    }
}
```

(출력)
상품명 : 신라면
단가 : 1000
입고 : 2026/03/01
상품명 : 짜파게티
단가 : 500
입고 : 2026/03/10
하하하
상품명 : null
단가 : 500
입고 : null
상품 2를 배열로 다뤄 봅니다.
1번째 상품 정보
상품명 : 소이조이
단가 : 2000
입고 : 2025/08/15
2번째 상품 정보
상품명 : 맥심커피
단가 : 500
입고 : 2025/07/17

#배열에 객체 이용 (초기화 기법)
(main클래스)
```java
// 배열 정의 (초기화 기법) //초기화 기법의 객체 생성 문법 : new연산자 클래스명
Product03[] productArray = {
		new Product03("쭈쭈바", 1500, "2025/12/25"),
		new Product03("사과", 3000, "2025/06/06"),
		new Product03("오징어땅콩", "2025/07/17")
};
```

```java
for (int i = 0; i < itemlist.length; i++) {
	System.out.println((i+1) + "번째 상품 정보");
	productArray[i].display();
}
```
(클래스)
위와 동일

(출력)
1번째 상품 정보
상품명 : 쭈쭈바
단가 : 1500
입고 : 2025/12/25
2번째 상품 정보
상품명 : 사과
단가 : 3000
입고 : 2025/06/06
3번째 상품 정보
상품명 : 오징어땅콩
단가 : 500
입고 : 2025/07/17

# 4. 12:30 ~ 13:20
# 클래스로 구현해볼 내가 생각해온 것으로 여러가지 해보기
(main클래스)
```java
package ch04_class;
```

```java
public class exProductMain02 {
    static void main() {
        //객체 생성 및 메소드 이용 출력
        exProduct02 tagHeuer = new exProduct02("tagHeuer","automatic", 38,"stainLess");
        tagHeuer.display();
```

```java
        // 객체 이용해서 배열 생성 (new 연산자 기법)
        exProduct02[] watch = new exProduct02[4];
```

```java
        // 줄바꿈 생성 3개
        System.out.println("");
        System.out.println("");
        System.out.println("");
```

```java
        // 제목 만들기01
        System.out.println("시계 리스트 (new연산자 기법)");
```

```java
        // 배열 요소에 값 할당
        watch[0] = new exProduct02("tagHeuer_Aqua_racer", "automatic", 38, "stainLess");
        watch[1] = new exProduct02("seiko_Alpinist", "automatic", 72, "stainLess" );
        watch[2] = new exProduct02("tissot_Le_locle", "automatic", 80, "stainLess");
        watch[3] = new exProduct02("citizen_Attesa", "quartz", 4380, "titanium");
```


```java
        // 반복문 이용해서 출력
        for (int i = 0; i < watch.length; i++) {
            watch[i].display();
            System.out.println("");
        }
```

```java
        // 제목 만들기02
        System.out.println("시계 리스트 (초기화 기법)");
```

```java
        // 객체 이용해서 배열 생성 (초기화 기법)
        exProduct02[]watch2 = {
            new exProduct02("tagHeuer_Aqua_racer", "automatic", 38, "stainLess"),
            new exProduct02("seiko_Alpinist", "automatic", 72, "stainLess" ),
            new exProduct02("tissot_Le_locle", "automatic", 80, "stainLess"),
            new exProduct02("citizen_Attesa", "quartz", 4380, "titanium")
        };
```

```java
        // 반복문
        for (int i = 0; i < watch2.length; i++) {
            watch2[i].display();
            System.out.println("");
        }
```

```java
    }
}
```

(클래스)
```java
package ch04_class;
```

```java
public class exProduct02 {
    // 맴버 변수 생성 // 기본값 설정
    String name = "no_info";
    String movement = "no_info";
    int power_reserve = 0;
    String material = "no_info";
```

```java
    // 메소드 생성 // 매개변수 4개
    exProduct02(String name, String movement, int power_reserve, String material){
        this.name = name;
        this.movement = movement;
        this.power_reserve = power_reserve;
        this.material = material;
    }
```

```java
    // 메소드 생성
    public void display() {
        System.out.println("이름 : " + name);
        System.out.println("구동 방식 : " + movement);
        System.out.println("파워리저브 : " + power_reserve + "hours");
        System.out.println("재질 : " + material);
    }
}
```

(출력)
이름 : tagHeuer
구동 방식 : automatic
파워리저브 : 38hours
재질 : stainLess


시계 리스트 (new연산자 기법)
이름 : tagHeuer_Aqua_racer
구동 방식 : automatic
파워리저브 : 38hours
재질 : stainLess

이름 : seiko_Alpinist
구동 방식 : automatic
파워리저브 : 72hours
재질 : stainLess

이름 : tissot_Le_locle
구동 방식 : automatic
파워리저브 : 80hours
재질 : stainLess

이름 : citizen_Attesa
구동 방식 : quartz
파워리저브 : 4380hours
재질 : titanium

시계 리스트 (초기화 기법)
이름 : tagHeuer_Aqua_racer
구동 방식 : automatic
파워리저브 : 38hours
재질 : stainLess

이름 : seiko_Alpinist
구동 방식 : automatic
파워리저브 : 72hours
재질 : stainLess

이름 : tissot_Le_locle
구동 방식 : automatic
파워리저브 : 80hours
재질 : stainLess

이름 : citizen_Attesa
구동 방식 : quartz
파워리저브 : 4380hours
재질 : titanium

# 5. 14:20 ~ 15:10
강아지
name
age
guide //안내견 여부
eat()
bark()

고양이
name
age
indoor // 애완, 길고양이
eat()
bark()

동물
name
age
eat()
bark()

총 4개의 클래스
각 해당 클래스1개씩 (Animal, Dog, Cat)
+ 메인 클래스 1개 (AnimalMain)

# 일반화
공통분모는 위로 올리고 공통적이지 않은 분모는 아래로 내림
1) Animal 클래스에 공통분모를 넣고
(Animal클래스)
```java
package ch05_inheritance;
```

```java
public class Animal {
    private String name;
    private int age;
```

```java
    // eat(), bark()
}
```

2) Dog클래스와 Cat클래스에 공통적이지 않은 분모를 넣음
(Dog클래스)
```java
package ch05_inheritance;
```

```java
public class Dog {
    boolean guide; //안내경 여부
}
```

(Cat클래스)
```java
package ch05_inheritance;
```

```java
public class Cat {
    boolean indoor; //애완묘, 길고양이
}
```


#상속
탄생 배경 : 계속해서 클래스를 만들지 말고 비슷한 것은 이미 만들어진 것에서 그 비슷한 특성을 물려받으면 중복할 일이 없지 않을까?
수퍼 클래스 - 부모 클래스 = 상위 클래스 = 상속을 해주는 클래스
서브클래스 - 자식 클래스 = 하위 클래스 = 상속을 받는 클래스

문법 :
```java
접근지정자 class 서브클래스 extends 수퍼클래스{
	//어떤 코드
}
```

ex)
```java
public class Dog extends Animal {
	boolean guide; //안내경 여부
}
```

- 실제 해보기01
```java
package ch05_inheritance;
```

```java
public class AnimalMain {
	static void main() {
		// 클래스이름 객체이름 = new 생성자이름();
		Dog retriver = new Dog();
		retriver. //내용물을 보면 Dog클래스 안에 있는 맴버 변수말고도 Animal클래스에서 상속받은 맴버 변수도 있음
	}
}
```

- 실제 해보기02
(main클래스)
```java
package ch05_inheritance;
```

```java
public class AnimalMain {
    static void main() {
        // 클래스이름 객체이름 = new 생성자이름();
        Dog retriver = new Dog();
        retriver.name = "김리트리버";
        retriver.age = 2;
        retriver.guide = false;
```

```java
        Dog maltese = new Dog();
        maltese.name = "이말티즈";
        maltese.age = 7;
        maltese.guide = true;
```

```java
        //고양이 : 페르시안 (persian), 샴(siamese)
```

```java
        Cat persian = new Cat();
        persian.name = "박페르시안";
        persian.age = 6;
        persian.indoor = false;
```

```java
        Cat siamese = new Cat();
        siamese.name = "최샴";
        siamese.age = 7;
        siamese.indoor = true;
    }
}
```

(Animal클래스)
```java
package ch05_inheritance;
```

```java
public class Animal {
    String name;
    int age;
```

```java
    // eat(), bark()
}
```

(Dog클래스)
```java
package ch05_inheritance;
```

```java
// 상속을 해주는 Animal 클래스를 Dog 클래스가 상속을 받습니다.
// class 서브클래스 extends 수퍼클래스
// 수퍼클래스 = 부모 클래스
// 서브클래스 = 자식 클래스
public class Dog extends Animal {
    boolean guide; //안내경 여부
}
```

(Cat클래스)
```java
package ch05_inheritance;
```

```java
public class Cat extends Animal {
    boolean indoor; //애완묘, 길고양이
}
```

# 6. 15:20 ~ 16:10
수퍼 클래스는 서브 클래스에 참조할 수 없음
서브 클래스는 수퍼 클래스에 참조할 수 있음
-> display메소드를 만들때 모든 정보를 다 참조할 수 있는 서브 클래스에 만들면 됨
But 수퍼클래스에서 이용할 수 있는 정보를 가지고 display가능
ex)
(Animal클래스)(수퍼클래스)
```java
package ch05_inheritance;
```

```java
public class Animal {
    String name;
    int age;
```

```java
    // eat(), bark()
```

```java
    // 반환타입 메소드이름(매개변수){...}
    public void display(){
        System.out.println("이름 : " + name);
        System.out.println("나이 : " + age);
    }
}
```

(main클래스)
```java
package ch05_inheritance;
```

```java
public class AnimalMain {
    static void main() {
        // 클래스이름 객체이름 = new 생성자이름();
        Dog retriver = new Dog();
        retriver.name = "김리트리버";
        retriver.age = 2;
        retriver.guide = false;
        retriver.display();
```

```java
        Dog maltese = new Dog();
        maltese.name = "이말티즈";
        maltese.age = 7;
        maltese.guide = true;
        maltese.display();
```

```java
        //고양이 : 페르시안 (persian), 샴(siamese)
```

```java
        Cat persian = new Cat();
        persian.name = "박페르시안";
        persian.age = 6;
        persian.indoor = false;
        persian.display();
```

```java
        Cat siamese = new Cat();
        siamese.name = "최샴";
        siamese.age = 7;
        siamese.indoor = true;
        siamese.display();
    }
}
```

(출력)
이름 : 김리트리버
나이 : 2
이름 : 이말티즈
나이 : 7
이름 : 박페르시안
나이 : 6
이름 : 최샴
나이 : 7

# 오버라이딩
부모에게 물려받은 메소드를 자식 클래스에 맞게 재정의하는 것
위에서 언급했던 자식 클래스에 추가 수정 가능여부

문법 :
```java
접근지정자  void 메소드(){
	super.메소드();
}
```

(예시)
```java
public void display(){
        super.display();
```

```java
package ch05_inheritance;
```

```java
// 상속을 해주는 Animal 클래스를 Dog 클래스가 상속을 받습니다.
// class 서브클래스 extends 수퍼클래스
// 수퍼클래스 = 부모 클래스
// 서브클래스 = 자식 클래스
public class Dog extends Animal {
    boolean guide; //안내경 여부
```

```java
	// 이 부분을 annotation이라고 부르고 고급? 주석이라고도 부름
	// 단순히 이 메소드는 오버라이딩되었다고 알리는 것. // 사실상 없어도 됨
    @Override
    public void display() {
        System.out.println("안내견 여부 : " + guide); // Dog클래스만의 메소드를 추가함(오버라이딩 함)
```

```java
        // super는 나의 수퍼 클래스 Animal을 의미합니다.
		// super(Animal)의 display를 호출해서 가져옴 (프린트 name과 age)
        super.display();
    }
}
```

# 7. 16:20 ~ 17:10
# 오버라이딩 (if선택문 이용)
(Dog클래스)
```java
package ch05_inheritance;
```

```java
// 상속을 해주는 Animal 클래스를 Dog 클래스가 상속을 받습니다.
// class 서브클래스 extends 수퍼클래스
// 수퍼클래스 = 부모 클래스
// 서브클래스 = 자식 클래스
public class Dog extends Animal {
    boolean guide; //안내경 여부
```

    @Override // 이 메소드는 오버라이딩되었습니다.
```java
    public void display() {
        // super는 나의 수퍼 클래스 Animal을 의미합니다.
        super.display();
```

```java
        if(guide){
            System.out.println("안내경 여부 : 적합");
        }else{
            System.out.println("안내견 여부 : 부적합");
        }
    }
}
```

(main클래스)
```java
package ch05_inheritance;
```

```java
public class AnimalMain {
    static void main() {
        // 클래스이름 객체이름 = new 생성자이름();
        Dog retriver = new Dog();
        retriver.name = "김리트리버";
        retriver.age = 2;
        retriver.guide = false;
        retriver.display();
```

```java
        Dog maltese = new Dog();
        maltese.name = "이말티즈";
        maltese.age = 7;
        maltese.guide = true;
        maltese.display();
```

(출력)
이름 : 김리트리버
나이 : 2
안내견 여부 : 부적합
이름 : 이말티즈

# 질문내용*중요*
Animal클래스에
```java
public void bark(){
```

```java
}
를 적고
Dog클래스에 오버라이드할때
원래는 public void bark(){
        super.bark();
    }
인데
--------------------------------
public void bark(){
	System.out.println(name + "이(가) 짖습니다.");
}
	---------------------------------------
를 의미하는 super.bark();를 제거해서 나온
public void bark(){
```

```java
}
이것은 과연 오버라이드라고 부를 수 있는가?
```

(답)
오버라이드임
왜냐하면
상속에 의해 Animal이 수퍼 클래스이고 Dog가 서브 클래스인데
이 말은 사실상 수퍼 클래스인 Animal의 데이터들이 서브 클래스인 Dog에 이미 들어가 있다는 말인데
이 말은 Dog클래스 안에 이미
```java
public void bark(){
```

```java
}
이 메소드가 들어가 있다는 말인데 (Dog클래스의 안에 눈으로는 안보이지만)
```

```java
public void bark(){
```

```java
}
만약 눈에 보이는 이 메소드가 오버라이드가 아니고 그냥 Dog클래스의 메소드였다면
논리적으로 똑같은 이름의 bark메소드가 있다는 말이고 이것은 시스템의 에러를 유발함
그런데 에러없이 프로그램이 돌아가는데 그 이유가 컴파일러?가 인식하기를 bark() 메소드를 Dog클래스의 메소드라고 인식하지 않고
오버라이드된 bark() 메소드라고 인식했기 때문임
```


# 8. 17:20 ~ 18:10

상속의 계보? 최고 위의 수퍼 클래스는 object(오브젝트)임
따라서 Animal클래스는 Dog와 Cat의 수퍼클래스이면서 오브젝트의 서브클래스임
명시적으로 상속을 받은 것이 아니라 암시적으로 상속을 받음
명시적 상속은 (extends이용하는 것)

가장 많이 쓰는 오브젝트의 메소드가 toString()임 (반환타입이 String인->객체정보를 문자열로 만들어서 출력해줌

(main클래스)
```java
System.out.println(retriver.toString());
```

(출력)
ch05_inheritance.Dog@85ede7b

(풀이)
retriver는 ch05_inheritance패키지 안에 있는 Dog로 만든 객체이다. @ (16진수의 수) 객체의 해쉬코드 값을 16진수로 표현


오브젝트의 메소드를 오버라이드 해보기
(Animal클래스)
(불러오기)
@Override
```java
public String toString() {
	return super.toString();
}
```

(출력)
ch05_inheritance.Dog@85ede7b

(오버라이드 하기)
@Override
```java
public String toString() {
	String imsi = "이름 : " + name + ", 나이 : " + age + ", 먹이 : " + feed ;
	return imsi ;
}
```

(출력)
이름 : 김리트리버, 나이 : 2, 먹이 : 건식 사료

#  메소드 은닉화
오버라이딩시 본의 아니게 부모 메소드 접근이 막히는 현상

예시:
원래 오브젝트의 메소드였던 toString을 서브클래스인 Animal클래스에서 오버라이드하고
그 후에 Animal클래스의 서브 클래스인 Dog클래스에서 toString메소드를 호출하려고할때 오브젝트의 toString메소드를 호출하지 못하고
논리적으로 더 가까운 Animal클래스에서 오버라이드된 toString이 호출되어버림

				toString()
Object			O
Animal			O
Dog				X
