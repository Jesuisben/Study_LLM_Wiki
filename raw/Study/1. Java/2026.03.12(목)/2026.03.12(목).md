# 1. 09:30 ~ 10:20
# 향상된 for / 확장 for 구문
배열, 컬렉션에 저장된 요소들을 처음부터 끝까지 하나씩 순차적으로 꺼낼 때 사용하는 반복문
인덱스 관리 필요X -> 코드 간결, 실수 줄여줌
단순히 전체를 보거나 출력할때 사용

- 원래의 for 반복문 문법
```java
for(초기식 ; 조건식 ; 증감식){
	반복할 구문 ;
}
```

- 향상된 for / 확장 for 구문 문법
```java
// 타입 : 배열요소의 타입 / 복수이름 : 배열이름 / 단수이름 : 제어변수 i같은 느낌으로 생각하기
for(타입 단수이름 : 복수이름){
	반복할 구문 ;
}
```

(ch03_array패키지 - Array01클래스 내용에 추가함)

(실습)
```java
String[] bts = new String[7];
```

```java
bts[0] ="진";
bts[1] ="뷔";
bts[2] ="정국";
bts[3] ="rm";
bts[4] ="지민";
bts[5] ="슈가";
bts[6] ="제이홉";
```

```java
// 선입 선출(FIFO), 반대 LIFO(프링글스)
System.out.println("향상된 for 구문");
// for(타입 단수이름 : 복수이름){
for(String item : bts){
	System.out.println(item);
}
```

(출력)
진
뷔
정국
rm
지민
슈가
제이홉

- 일반 for와 확장 for와 적용 차이
일반 for :
숫자?가 중요할때
명확한 목적이 있을때?

확장 for :
선입선출로 데이터 값을 나열할때(먼저 들어간 데이터값을 먼저 출력할때)

# 선입선출 / 후입선출
1) 선입선출(FIFO)
먼저 들어온 데이터가 먼저 나감 (자료구조 : Queue)
ex) 프린터 인쇄 대기열
거의 대부분의 자료구조가 여기에 해당됨

2) 후입선출(LIFO)
마지막에 들어온 데이터가 가장 먼저 나감 (자료구조 : Stack)
ex) 쌓여 있는 접시 꺼내기

# 다른 패키지에 접근
```java
import ch04_class.Product01; // ch04_class라는 패키지의 Product01이라는 클래스를 가져오기
다른 패키지에서 가져올 데이터가 public이여야함
```

(실습)
```java
package ch06_abstract_interface;
```

```java
// 동일한 패키지가 아니면 import 구문을 사용해야 합니다.
// 반드시 public이어야 합니다.
// 클래스 내의 변수/메소드 또한 public이 아니면 접근이 불가능합니다.
```

```java
import ch04_class.Product01;
```

```java
public class MyProduct {
    static void main(String[] args) {
        Product01 apple = new Product01();
```

```java
        apple.name = "사과";
```

```java
        System.out.println("품목이름 : " + apple.name);
    }
}
```

(출력)
품목이름 : 사과

# 2. 10:30 ~ 11:20
ch06_abstract_interface의 하위 패키지 (cafeinterface 패키지 생성)
cafeinterface 패키지에 file 생성 (file 이름 : 구성 내용.txt)
(작성)
수퍼 클래스 : Beverage05

서브 클래스
    Americano05
    Espresso05
    Latte05

메인 클래스 : InheTest05

# 배열 이용해서 객체 생성
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class InheTest05 {
    static void main(String[] args) {
        // 부모 타입을 사용한 배열 생성
        Beverage05[] orderList = {
                new Americano05("아메리카노", 4000.0, 200)
        };
```

```java
        // 이것도 승급(UpCasting)임
        // 클래스명 객체명 = new연산자 생성자();
        // 타입[] 배열명 = {배열요소};
        // println(타입[]); = 배열요소 // 이거 자체가 사실상 변수라고 봐도 됨
        // 따라서 사실상 타입[] 자체가 객체임
```

```java
        System.out.println("향상 for를 사용한 출력");
        for(Beverage05 item : orderList){
            System.out.println("--------------------------------------");
            System.out.println(item);
            System.out.println();
        }
    }
}
```

+ 어제 했던 그대로 toString을 오버라이드 오버라이드해서 출력

# 3. 11:30 ~ 12:20
# 추상 메소드
공통적인 특징(어느정도의 가이드라인)을 모으지만
구체적인 내용은 자식에게 맡기기 위해서 필요함
+ 서브 클래스들에게 강제성?을 부여하기 위해서

무언가 행동이나 기능을 해야한다고만 전달하고 구체적인 내용은 제시하지 않는 메소드
"너 뭔가 해야해!! 근데 그건 너가 정해!"라고 수퍼 클래스가 서브 클래스한테 말하는 메소드

결과 : 강제적인 표준규범을 만듬 (표준 규범이 없으면 서로다른 개발자들끼리 여러 이름의 메소드로 만들어서 유지보수가 힘듬)

문법 :
1) 수퍼 클래스의 형식 변경
추상 메소드가 들어간 클래스는 그 자체가 반드시 추상 클래스여야 한다
클래스에 abstract 추가
ex)
```java
public abstract class Beverage05
```

2) 수퍼 클래스의 메소드 형식 변경
```java
클래스의 일반 메소드 만드는 거에서 abstract추가, 중괄호 삭제 후 ;(세미콜론) 추가
접근지정자 abstract 타입 메소드명();
ex)
public abstract void drink();
```

3) 서브 클래스에 (수퍼 클래스의 추상 메소드를 구체화하는) 메소드 추가
문법 : (일반 메소드 만들때와 동일)
```java
접근지정자 타입 메소드명(){
```

```java
}
```

서브 클래스에 (수퍼 클래스의 추상 메소드를 구체화하는) 메소드를 추가하지 않으면 오류가 남
-> 이래서 "강제성"을 부여한다라고 표현함.


(실습)
(Beverage05수퍼클래스, 추상클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public abstract class Beverage05 {
    private String name;
    private double price;
```

```java
    // 모든 음료는 마십니다.
    public abstract void drink();
```

```java
    public String getName() {
        return name;
    }
```

    @Override
```java
    public String toString() {
        String imsi = "이름 : " + name + ", 가격 : " + price;
        return imsi;
    }
```

```java
    public Beverage05() {
    }
```

```java
    public Beverage05(String name, double price) {
        this.name = name;
        this.price = price;
    }
}
```

(Americano05서브클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class Americano05 extends Beverage05{
    private double waterAmount;
```

    @Override
```java
    public void drink() {
        System.out.println("물의 양이 " + waterAmount + "ml인 " + super.getName() + "를 뜨거운 물과 함께 마십니다.");
    }
```

    @Override
```java
    public String toString() {
        return super.toString() + ", 물의 양 : " + waterAmount;
    }
```

```java
    public Americano05(String name, double price, double waterAmount) {
        super(name, price);
        this.waterAmount = waterAmount;
    }
}
```

(Espresso05서브클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class Espresso05 extends Beverage05{
    private int shotCount;
```

    @Override
```java
    public void drink(){
        System.out.println("샷개수 " + shotCount + "인 " + super.getName() + "을 진하게 한모금 마십니다.");
    }
```

```java
    public Espresso05(String name, double price, int shotCount ){
        super(name, price);
        this.shotCount = shotCount;
    }
```

    @Override
```java
    public String toString() {
        return super.toString() + ", 샷 추가 갯수 : " + shotCount;
    }
}
```

(Latte05서브클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class Latte05 extends Beverage05{
    private String milkType;
```

    @Override
```java
    public void drink(){
        System.out.println("우유의 타입이 " + milkType + "인 " + super.getName() + "을 포근하게 먹습니다.");
    }
```

```java
    public Latte05(String name, double price, String milkType ){
        super(name, price);
        this.milkType = milkType;
    }
```

    @Override
```java
    public String toString(){
        return super.toString() + ", 우유의 타입 : " + milkType;
    }
}
```

(main클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class InheTest05 {
    static void main(String[] args) {
        // 부모 타입을 사용한 배열 생성
        Beverage05[] orderList = {
                new Americano05("아메리카노", 4000.0, 200),
                new Espresso05("에스프레소", 3000.0, 1),
                new Latte05("라떼", 6000.0, "아몬드 우유")
        };
```

```java
        // 이것도 승급(UpCasting)임
        // 클래스명 객체명 = new연산자 생성자();
        // 타입[] 배열명 = {배열요소};
        // println(타입[]); = 배열요소 // 이거 자체가 사실상 변수라고 봐도 됨
        // 따라서 사실상 타입[] 자체가 객체임
```

```java
        System.out.println("향상 for를 사용한 출력");
        for(Beverage05 item : orderList){
            System.out.println("--------------------------------------");
            System.out.println(item);
            item.drink();
            System.out.println();
        }
    }
}
```

(출력)
향상 for를 사용한 출력
--------------------------------------
이름 : 아메리카노, 가격 : 4000.0, 물의 양 : 200.0
물의 양이 200.0ml인 아메리카노를 뜨거운 물과 함께 마십니다.

--------------------------------------
이름 : 에스프레소, 가격 : 3000.0, 샷 추가 갯수 : 1
샷개수 1인 에스프레소을 진하게 한모금 마십니다.

--------------------------------------
이름 : 라떼, 가격 : 6000.0, 우유의 타입 : 아몬드 우유
우유의 타입이 아몬드 우유인 라떼을 포근하게 먹습니다.

# 4. 12:30 ~ 13:20
# interface (다중상속)
cafeinterface 패키지 안에 WaterAdjustable, ShotAddable 인터페이스 생성
인터페이스 내의 모든 메소드는 모두 당연하게 추상 메소드라서
따로 추상 메소드 형식으로 안만들어도 자동으로 추상 메소드가 됨
-> 서브클래스에 강제적으로 메소드를 작성해야함 (구체화 : implementation)

문법 :
1) 서브클래스 extends 수퍼클래스 implements 인터페이스
```java
public class Americano05 extends Beverage05 implements WaterAdjustable
```

2) 인터페이스의 메소드 형식
```java
클래스의 일반 메소드 만드는 거에서 중괄호 삭제 후 ;(세미콜론) 추가
접근지정자 타입 메소드명();
ex)
void adjustWater(double amount);
```

-> 원래 문법 // abstract은 생략해도됨 (인터페이스에서는 자동으로 추상 메소드로 인식해서)
```java
public abstract void adjustWater(double amount);
```

3) 서브 클래스에 (수퍼 클래스의 추상 메소드를 구체화하는) 메소드 추가 (오버라이드)
문법 : (일반 메소드 만들때와 동일)
```java
접근지정자 타입 메소드명(){
	사용할 구문
}
```

ex)
@Override
```java
public void adjustWater(double amount) {
	this.waterAmount += amount;
	System.out.println("물 추가 : " + amount + "ml");
	System.out.println("현재 물의 양 : " + this.waterAmount);
}
```

(실습)
(WaterAdjustable인터페이스)
```java
void adjustWater(double amount); // 물 조절하기
```

(ShotAddable인터페이스)
```java
void addShot(int count); // 샷 추가하기
```

(MilkAddable인터페이스)
```java
public abstract void addMilk(String milkType, double amount);
```

(Americano05서브클래스)
@Override
```java
public void adjustWater(double amount) {
	this.waterAmount += amount;
	System.out.println("물 추가 : " + amount + "ml");
	System.out.println("현재 물의 양 : " + this.waterAmount);
}
```

(Espresso05서브클래스)
@Override
```java
public void addShot(int count){
	this.shotCount += count;
	System.out.println("샷 추가 : " + count + "샷");
	System.out.println("현재 샷 추가 : " + this.shotCount + "샷");
}
```

(Latte05서브클래스)
@Override
```java
public void addMilk(String milkType, double amount){
	this.milkType = this.milkType + ", " + milkType;
	System.out.println("우유 추가 : " + milkType);
	System.out.println("추가된 우유의 양 " + amount + "ml");
}
```

(main클래스)
```java
System.out.println("향상 for를 사용한 출력");
for(Beverage05 item : orderList){
	System.out.println("--------------------------------------");
	System.out.println(item);
```

```java
	if(item instanceof Americano05){
```
		((Americano05)item).adjustWater(20); // 강등(DownCasting)

```java
	}else if(item instanceof Espresso05){
```
		((Espresso05)item).addShot(1); // 강등(DownCasting)

```java
	}else if(item instanceof Latte05){
```
		((Latte05)item).addMilk("아몬드 우유", 100); // 강등(DownCasting)
```java
	}
```

```java
	item.drink();
	System.out.println();
}
```

(출력)
향상 for를 사용한 출력
--------------------------------------
이름 : 아메리카노, 가격 : 4000.0, 물의 양 : 200.0
물 추가 : 20.0ml
현재 물의 양 : 220.0
물의 양이 220.0ml인 아메리카노를 뜨거운 물과 함께 마십니다.

--------------------------------------
이름 : 에스프레소, 가격 : 3000.0, 샷 추가 갯수 : 1
샷 추가 : 1샷
현재 샷 추가 : 2샷
샷 개수 2인 에스프레소을 진하게 한모금 마십니다.

--------------------------------------
이름 : 카페라떼, 가격 : 4500.0, 우유의 타입 : 바나나 우유
우유 추가 : 아몬드 우유
추가된 우유의 양 100.0ml
우유의 타입이 바나나 우유, 아몬드 우유인 카페라떼을 포근하게 먹습니다.

# 5. 14:20 ~ 15:10
문제 풀이

# 6. 15:20 ~ 16:10
# 한개의 서브 클래스에 1개의 수퍼클래스와 3개의 인터페이스 다중상속 해보기
(실습)
(SpecialCoffee05서브클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class SpecialCoffee05 extends Beverage05 implements WaterAdjustable, ShotAddable, MilkAddable {
```

```java
    private double waterAmount;
    private int shotCount;
    private String milkType;
```

```java
    public SpecialCoffee05(String name, double price, double waterAmount, int shotCount, String milkType) {
        super(name, price);
        this.waterAmount = waterAmount;
        this.shotCount = shotCount;
        this.milkType = milkType;
    }
```

    @Override
```java
    public void drink(){
        System.out.println(super.getName() + "을(를) 마십니다.");
    }
```

    @Override
```java
    public void changeMilk(String milkType){
        System.out.println("타입 변경 이전 : " + this.milkType + ", 이후 : " + milkType);
        this.milkType = milkType;
    }
```

    @Override
```java
    public void addShot(int count){
        this.shotCount += count;
        System.out.println("샷이 " + count + "개 추가되어 " + shotCount + "개입니다.");
    }
```

    @Override
```java
    public void adjustWater(double amount){
        this.waterAmount += amount;
        System.out.println("물 " + amount + "ml를 추가하였습니다.");
    }
}
```

(WaterAdjustable인터페이스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
// 물의 양을 조절할 수 있도록 설계한 인터페이스
public interface WaterAdjustable {
    // 인터페이스 내의 모든 메소드는 추상 메소드입니다.
    void adjustWater(double amount); // 물 조절하기
```

```java
}
```

(ShotAddable인터페이스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
// 샷의 개수를 조절할 수 있도록 설계한 인터페이스
public interface ShotAddable {
    void addShot(int count); // 샷 추가하기
}
```

(MilkAddable인터페이스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public interface MilkAddable {
    // 우유의 타입을 변경하는 추상 메소드
    // 고객이 타입을 바꿔달라고 요청하는 경우
    public abstract void changeMilk(String milkType);
}
```

(Beverage05수퍼클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public abstract class Beverage05 {
    private String name;
    private double price;
```

```java
    // 모든 음료는 마십니다.
    public abstract void drink();
```

```java
    public String getName() {
        return name;
    }
```

    @Override
```java
    public String toString() {
        String imsi = "이름 : " + name + ", 가격 : " + price;
        return imsi;
    }
```

```java
    public Beverage05(){
    }
```

```java
    public Beverage05(String name, double price) {
        this.name = name;
        this.price = price;
    }
}
```

(main클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class InheTest05 {
    static void main(String[] args) {
        // 부모 타입을 사용한 배열 생성
        Beverage05[] orderList = {
                new Americano05("아메리카노", 4000.0, 200),
                new Espresso05("에스프레소", 3000.0, 1),
                new Latte05("카페라떼", 4500.0, "바나나 우유"),
                new SpecialCoffee05("스페셜커피", 6000.0, 150, 2,"우유")
        };
```

```java
        // 이것도 승급(UpCasting)임
        // 클래스명 객체명 = new연산자 생성자();
        // 타입[] 배열명 = {배열요소};
        // println(타입[]); = 배열요소 // 이거 자체가 사실상 변수라고 봐도 됨
        // 따라서 사실상 타입[] 자체가 객체임
```

```java
        System.out.println("향상 for를 사용한 출력");
        for(Beverage05 item : orderList){
            System.out.println("--------------------------------------");
            System.out.println(item);
```

```java
            if(item instanceof Americano05){
```
                ((Americano05)item).adjustWater(20); // 강등(DownCasting)

```java
            }else if(item instanceof Espresso05){
```
                ((Espresso05)item).addShot(1); // 강등(DownCasting)

```java
            }else if(item instanceof Latte05){
```
                ((Latte05)item).changeMilk("아몬드 우유"); // 강등(DownCasting)

```java
            }else if(item instanceof SpecialCoffee05){
                ((SpecialCoffee05)item).addShot(1);
                ((SpecialCoffee05)item).adjustWater(50);
                ((SpecialCoffee05)item).changeMilk("아몬드 우유");
```

```java
            }
```

```java
            item.drink();
            System.out.println();
        }
    }
}
```

(출력)
향상 for를 사용한 출력
--------------------------------------
이름 : 아메리카노, 가격 : 4000.0, 물의 양 : 200.0
물 추가 : 20.0ml
현재 물의 양 : 220.0
물의 양이 220.0ml인 아메리카노를 뜨거운 물과 함께 마십니다.

--------------------------------------
이름 : 에스프레소, 가격 : 3000.0, 샷 추가 갯수 : 1
샷 추가 : 1샷
현재 샷 추가 : 2샷
샷 개수 2인 에스프레소을 진하게 한모금 마십니다.

--------------------------------------
이름 : 카페라떼, 가격 : 4500.0, 우유의 타입 : 바나나 우유
우유의 타입이 바나나 우유에서 아몬드 우유으로 변경되었습니다.
우유의 타입이 아몬드 우유인 카페라떼을 포근하게 먹습니다.

--------------------------------------
이름 : 스페셜커피, 가격 : 6000.0
샷이 1개 추가되어 3개입니다.
물 50.0ml를 추가하였습니다.
타입 변경 이전 : 우유, 이후 : 아몬드 우유
스페셜커피을(를) 마십니다.


# 가게 이름 설정 (static 변수 이용)
전체에 영향을 끼치는 클래스가 수퍼클래스인 Beverage05클래스여서 이 클래스 안에 맴버 변수(public)으로 String 생성
-> But! 이렇게 하면 객체를 생성해서 맴버 변수를 호출해야함
-> 가게 이름만 출력하는데 굳이 객체까지 필요는 없어서 객체를 따로 생성을 안하고 호출하는 방법으로 맴버 변수가 아닌 static변수를 사용함
static변수는 클래스 변수라고도 불림.
*맴버 변수는 반드시 객체에 참조 연산자를 이용해서 호출해야함.*

(Beverage05수퍼클래스)
```java
// 카페 이름은 객체 생성하지 않아도 접근이 가능하도록 하는 것이 좋겠습니다.
// (static을 사용하면 객체 없이 사용가능)
public static String STORE_NAME = "G-CAFE";
```


(main클래스)
```java
// 가게 이름만 출력하는데 굳이 객체까지 필요 없어서 객체 생성은 안함
System.out.println("어서 오세요~~" + Beverage05.STORE_NAME + "입니다.");
```

(출력)
어서오세요~~G-CAFE입니다.

# 가게 이름 설정 (static, final 이용)
(Beverage05수퍼클래스)
```java
// final이 붙어 있으면 편집 불가능한 상수(constant)입니다.
public static final String STORE_NAME = "G-CAFE"; // final 추가
```

(main클래스)
```java
// final이 붙은 변수여서 편집 불가능하기에 에러가 발생함
Beverage05.STORE_NAME = "하하하";
System.out.println("어서 오세요~~" + Beverage05.STORE_NAME + "입니다.");
```

-> final이 붙은 변수는 더이상 변수라고 부르지 않고 "상수"라고 부름.

# private이고 static인 변수
```java
private static int beverageCount = 0; // 주문한 커피 잔수
```

```java
public static int getBeverageCount(){
	return beverageCount;
}
```

객체 없이 호출할 수는 있지만 함부로 접근하지 못하게 private으로 제한해두지만
이 변수를 사용하기 위해서 getter를 써줘서 후회적으로 사용할 수 있게 해줌.

# 7. 16:20 ~ 17:10
static이 붙은 메소드는 맴버 메소드가 아니라 static메소드라고 불리며
객체.메소드명으로 접근할 수도 있지만
클래스명.메소드명으로 접근도 가능하다.
-> 따라서 이 메소드를 사용하기 위해서 따로 객체를 생성하지 않아도 된다.

# 커피 잔수 호출 (중요함!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!)
1) 서브클래스의 객체를 main에 만든다는 행위가

```java
Beverage05[] orderList = {
		new Americano05("아메리카노", 4000.0, 200),
		new Espresso05("에스프레소", 3000.0, 1),
		new Latte05("카페라떼", 4500.0, "바나나 우유"),
		new SpecialCoffee05("스페셜커피", 6000.0, 150, 2, "우유")
};
```

위 행위인데

2) 위 행위를 만들기 위해서
(Americano05서브클래스)
```java
public Americano05(String name, double price, double waterAmount) {
	super(name, price);
	this.waterAmount = waterAmount;
}
이 코드가 실행되어야 하는데
이를 위해서 super(name, price)
```

(Beverage05수퍼클래스)
```java
public Beverage05(String name, double price) {
	this.name = name;
	this.price = price;
	beverageCount++;         // 커피 잔 수 카운팅을 위한 증감값 코드
}
```

즉, 위의 코드가 실행되어야하고 이것이 서브클래스의 객체를 main에 생성하기 위한 초기 단계이기에
이곳에 증감값을 넣으면 커피 잔 수 (main에 있는 서브 클래스들의 커피를 상징하는 객체의 수)를 알 수 있다.


# 커피 메뉴 추가해보기
(Cappuccino05서브클래스)생성
(FoamAddable인터페이스)생성

(Cappuccino05서브클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
// Beverage05와 MilkAddable 상속 받기
public class Cappuccino05 extends Beverage05 implements MilkAddable, FoamAddable {
    private String milkType;
    private double foamAmount; // 거품량
```

    @Override
```java
    public void drink() {
        System.out.println("거품의 양이 " + foamAmount + "인 " + super.getName() + "를 젠틀하게 먹습니다.");
    }
```

    @Override
```java
    public void changeMilk(String milkType) {
        System.out.println("우유의 타입이 " + this.milkType + "에서 " + milkType + "으로 변경되었습니다.");
        this.milkType = milkType;
    }
```

    @Override
```java
    public void addFoam(double amount){
        this.foamAmount += amount;
        System.out.println("거품량 추가 : " + amount);
        System.out.println("현재 거품의 양 : " + this.foamAmount);
    }
```

```java
    public Cappuccino05(String name, double price, String milkType, double foamAmount){
        super(name, price);
        this.milkType = milkType;
        this.foamAmount = foamAmount;
    }
}
```


(FoamAddable인터페이스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public interface FoamAddable {
    void addFoam(double amount);
}
```

(main클래스)
```java
package ch06_abstract_interface.cafeinterface;
```

```java
public class InheTest05 {
    static void main(String[] args) {
        // Beverage05.STORE_NAME = "하하하";
        System.out.println("어서 오세요~~" + Beverage05.STORE_NAME + "입니다.");
```

```java
        // 부모 타입을 사용한 배열 생성
        Beverage05[] orderList = {
                new Americano05("아메리카노", 4000.0, 200),
                new Espresso05("에스프레소", 3000.0, 1),
                new Latte05("카페라떼", 4500.0, "바나나 우유"),
                new Cappuccino05("카푸치노", 5000, "우유", 50),
                new SpecialCoffee05("스페셜커피", 6000.0, 150, 2, "우유")
        };
```

```java
        System.out.println("주문 잔수 : " + Beverage05.getBeverageCount());
```

```java
        // 이것도 승급(UpCasting)임
        // 클래스명 객체명 = new연산자 생성자();
        // 타입[] 배열명 = {배열요소};
        // println(타입[]); = 배열요소 // 이거 자체가 사실상 변수라고 봐도 됨
        // 따라서 사실상 타입[] 자체가 객체임
```

```java
        System.out.println("향상 for를 사용한 출력");
        for (Beverage05 item : orderList) {
            System.out.println("--------------------------------------");
            System.out.println(item);
```

```java
            if (item instanceof Americano05) {
```
                ((Americano05) item).adjustWater(20); // 강등(DownCasting)

```java
            } else if (item instanceof Espresso05) {
```
                ((Espresso05) item).addShot(1); // 강등(DownCasting)

```java
            } else if (item instanceof Latte05) {
```
                ((Latte05) item).changeMilk("아몬드 우유"); // 강등(DownCasting)

```java
            } else if (item instanceof SpecialCoffee05) {
                ((SpecialCoffee05) item).addShot(1);
                ((SpecialCoffee05) item).adjustWater(50);
                ((SpecialCoffee05) item).changeMilk("아몬드 우유");
```

```java
            } else if (item instanceof Cappuccino05){
                ((Cappuccino05)item).changeMilk("초코 우유");
                ((Cappuccino05)item).addFoam(50);
```

```java
            }
```

```java
            item.drink();
            System.out.println();
        }
    }
}
```

(출력)
어서 오세요~~G-CAFE입니다.
주문 잔수 : 5
향상 for를 사용한 출력
--------------------------------------
이름 : 아메리카노, 가격 : 4000.0, 물의 양 : 200.0
물 추가 : 20.0ml
현재 물의 양 : 220.0
물의 양이 220.0ml인 아메리카노를 뜨거운 물과 함께 마십니다.

--------------------------------------
이름 : 에스프레소, 가격 : 3000.0, 샷 추가 갯수 : 1
샷 추가 : 1샷
현재 샷 추가 : 2샷
샷 개수 2인 에스프레소를 진하게 한모금 마십니다.

--------------------------------------
이름 : 카페라떼, 가격 : 4500.0, 우유의 타입 : 바나나 우유
우유의 타입이 바나나 우유에서 아몬드 우유으로 변경되었습니다.
우유의 타입이 아몬드 우유인 카페라떼를 포근하게 먹습니다.

--------------------------------------
이름 : 카푸치노, 가격 : 5000.0
우유의 타입이 우유에서 초코 우유으로 변경되었습니다.
거품량 추가 : 50.0
현재 거품의 양 : 100.0
거품의 양이 100.0인 카푸치노를 젠틀하게 먹습니다.

--------------------------------------
이름 : 스페셜커피, 가격 : 6000.0
샷이 1개 추가되어 3개입니다.
물 50.0ml를 추가하였습니다.
타입 변경 이전 : 우유, 이후 : 아몬드 우유
스페셜커피을(를) 마십니다.

# 8. 17:20 ~ 18:10
# 내일 프로젝트 준비
